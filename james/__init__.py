"""JAMES SDK — author-facing namespace.

The runtime API lives in :mod:`core.ontology_packs` +
:mod:`core.lifecycle`. This top-level ``james`` package is the
**SDK author namespace** — it exposes scaffolding tools (the
``james.pack`` subpackage) and convenience re-exports for pack
authors.

For runtime usage, prefer the explicit imports from ``core.*``
(see :doc:`docs/ONTOLOGY_PACK_AUTHORING.md`).
"""
__version__ = "0.6.0a1"
