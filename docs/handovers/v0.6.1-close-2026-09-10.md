# v0.6.1 사이클 정식 마감 — 2026-09-10

> **다음 세션 진입점.** rule #6 에 따라 사이클 상태의 원본은 이 문서입니다.
> Phase 정의 원본은 `v0.6.2-restart-roadmap-2026-09-03.md` §2 가 계속
> 유효하고, 이 문서는 그 **Phase 4** 를 실행한 결과입니다.
>
> **태그 대상**: `main` 의 2026-09-10 시점 tip — 즉 이 문서와
> `docs/release_notes_v0.6.1.md` 를 담은 커밋. **여기에 sha 를 적지
> 않습니다**: 같은 커밋 안의 문서가 자기 sha 를 적으면 그 문서를
> 고치는 커밋이 다시 tip 을 옮겨 영원히 한 칸씩 어긋납니다
> (#1113 → #1114 에서 실제로 두 번 겪었습니다). 실제 sha 는 태그
> 자체가 보존합니다.
>
> **CI 수치**는 `5307ab3` 에서 측정된 값입니다. 이후 커밋은 전부
> 문서 전용이라 그대로 유효합니다.
> **CI**: `tests` / `lint` / `security-scan` 전부 success
> (`5,309 passed / 0 failed`, run 34349540062)
> **DOI**: 미발행 — operator 판단 (§6)

---

## 1. 한 줄 요약

v0.4.4 이후 **태그 없이 떠 있던 282 개 PR** 을 `v0.6.1` 하나로 닫습니다.
로드맵이 적어 둔 "190 PR" 은 하드닝 스트림만 센 값이었고, **v0.5 사이클도
태그가 없었기 때문에** 실제 미발행 구간은 그보다 넓습니다.

---

## 2. 실제 범위 — 로드맵의 "190" 은 부분집합이었습니다

`v0.4.4..main` 을 직접 세었습니다.

| 값 | 실측 |
|---|---|
| PR 개수 | **282** (`#831` – `#1112`) |
| 기간 | 2026-06-12 → 2026-09-09 |
| 파일 | 522 changed, **+90,289 / −5,838** |
| 마지막 태그 | `v0.4.4` (`04de834`, 2026-06-12) |

네 구간으로 갈립니다.

| 구간 | PR | 수 | 내용 |
|---|---|---:|---|
| v0.5 close + v0.6 entry | #831 – #911 | 81 | v0.5 사이클 마감 · Track F UI · SDK/G8 · 2-fork entry skeleton · rule #5 분할 7건 · 템플릿 엔진 |
| v0.6 / v0.6.1 하드닝 | #912 – #1076 | 165 | 에이전트 트랙 · LLM 라우팅 통합 · 챗 UX 재작성 · UI 8→5 · lifecycle arc · 이미지 OCR/비전 |
| 유휴기 유지보수 | #1077 – #1082 | 6 | DOI 계보 정정 · ruff · uuid7 결함 · CI 원인 정리 · 문서 동기화 |
| v0.6.2 재개 | #1083 – #1112 | 30 | CI 그린 복구 · CSP style-src 졸업 · CI 무시 목록 회수 |

**⚠️ 이름 대 범위의 불일치를 그대로 적습니다.** 태그 이름은 로드맵
Phase 4 가 지정한 `v0.6.1` 이지만, 그 태그가 담는 내용은 v0.5 마감 ·
v0.6 · v0.6.1 · v0.6.2 재개분 **전부**입니다. `v0.5` 를 소급 태깅해
계보를 복원할지는 **operator 판단**입니다 (§6) — DOI 계보에 영향을
주고, 이 프로젝트는 그 계보를 한 번 정정한 전례가 있습니다 (#1077).

---

## 3. 테마별 요약

서술은 `CHANGELOG.md` 의 `[0.6.1]` / `[0.6.0]` / `[0.5.0-close]` 절과
`docs/release_notes_v0.6.1.md` 에 있습니다. 여기서는 **어디를 보면
되는지**만 남깁니다.

| 테마 | PR | 근거 문서 |
|---|---|---|
| v0.5 사이클 마감 (B.5 온톨로지 · G3–G7 · a11y · 보안 헤더) | #841 – #862 | `v0.5-close-2026-06-12.md` |
| Track F mother-level UI (Time-Travel TT.a–d / Change Review CR.a–d) | #865 – #880 | `v0.6-entry-skeleton-2026-06-13.md` |
| SDK / 온톨로지 팩 (G8.a–c · SDK.a–c · CSP nonce) | #868 – #885 | 같은 문서 |
| 운영 하드닝 P1–P4 (신뢰 프록시 · HTTPS · 테넌트 · 온보딩/롤백/추론시각화/용어집) | #890 – #899 | `CHANGELOG.md` `[0.6.0]` |
| rule #5 모듈 분할 12건 | #900 – #908, #986, #993, #1083 | §4.3 |
| 템플릿(양식) 엔진 | #909, #916 | `v0.6-template-engine-close-2026-06-13.md` |
| 에이전트 트랙 (`core/agent_tools` · tool-use 루프 · `run_shell` 기본 OFF) | #918 – #921, #1039 – #1047 | memory `project_agent_tools_coding_feature` |
| LLM 라우팅 통합 + 모드별 측정 (5-phase) | #922, #961 – #990 | memory `project_routing_buildout_5phase_v18_7` |
| 챗 UX 전면 개편 + SEKOS/JAMES 명명 확정 | #927 – #960 | memory `project_sekos_james_naming_contract` |
| UI 8→5 페이지 통합 + 비주얼 회귀 하네스 | #998 – #1017 | `v0.6.1-session-close-2026-06-23.md` |
| **lifecycle live-consistency arc** | #1018 – #1027, #1033 – #1034 | §4.2 — **읽고 넘어갈 것** |
| 이미지 인제스트 / 비전 체인 수리 + heartbeat | #1062 – #1075 | `v0.6.1-session-close-2026-06-26.md` |
| CI 그린 복구 + CSP style-src 졸업 + 무시 목록 회수 | #1082 – #1112 | `v0.6.2-session-close-2026-09-{08,09}.md` |

---

## 4. 불변식 — 마감 시점 실측

로드맵이 요구한 "불변식 유지 여부"입니다. 전부 `v0.4.4..main` 전 구간에
대해 이번에 직접 측정했고, 이전 핸드오버 문장을 옮겨 적지 않았습니다.

### 4.1 `core/retrieval` — 0 라인, 예외 없음

```
git diff --stat v0.4.4..main -- core/retrieval core/retrieval_engine.py
→ (빈 출력)
```

282 개 PR 전 구간에서 **한 줄도 바뀌지 않았습니다.** rule #2 의 측정
게이트가 실제로 작동했다는 가장 강한 증거입니다.

### 4.2 `core/graph` traversal — 깨졌고, 그건 의도된 것입니다

**이전 핸드오버의 "0 라인 streak 유지" 문장을 복사하지 마세요.**
v0.6.1 은 이 streak 을 **측정 근거를 갖고, 의도적으로** 깼습니다.

| PR | 파일 | 성격 |
|---|---|---|
| #908 | `core/graph_engine.py` → 3-file package | 순수 분할 (rule #5), 동작 변경 0 |
| **#1021** | `core/graph_engine/constants.py` (+32) · `engine.py` (±6) | `relation_is_live()` 게이트를 **탐색 출력**에 적용 |
| **#1023** | `core/ontology.py` (+11) | 같은 게이트를 **그래프 스코어**에 적용 |
| **#1024** | `core/graph_engine/constants.py` (+52) | **T1 유효 윈도우** |
| **#1026** | `core/graph_snapshot.py` (+8) | **현재 상태 3D 스냅샷** |

왜 승인됐는가: 결정론적 프로브 #1020 이 **라이브 탐색이 lifecycle
status 를 무시**함을 먼저 측정으로 증명했습니다 — 무효화 / 대체 /
만료된 관계가 LLM 컨텍스트로 새고 있었고 (픽스처에서 3/3), `status` 는
시간여행 경로에서만 지켜지고 있었습니다. 즉 **캐스케이드가 라이브
계층에서 사실상 무효**였습니다. 킬스위치 `JAMES_DISABLE_STATUS_FILTER`
가 있고, 시간여행 경로 격리는 #1025 로 고정됐으며, 백로그 재측정
(#1028–#1032) 이 회귀 없음을 확인했습니다 — HR N=100 은 **바이트 동일**.

근거: `reports/research-runs/lifecycle-live-consistency-arc-20260622.md`
+ `docs/handovers/v0.6.1-measurement-fix-loop-2026-06-22.md`.

### 4.3 `core/reasoning` — 변경됨 (분할 + 신규 모드 + 라우팅)

26 파일 **+2,659 / −973**. 대부분은 rule #5 분할 (`reflect.py` 697 라인
→ 5-file, `meta.py` → 4-file, `pipeline_synth` → 3-file, `engine.py` →
`engine_routing.py`) 이고, 나머지는 신규 `modes/vision.py` 와 모드별
라우팅 wire (#972 / #977 / #990) 입니다. 라우팅 wire 3건은 각각 3-cell
paired 측정을 앞세웠고 operator 승인이 있었습니다.

### 4.4 rule #5 (모듈 20 KB 상한) — 예외 0건

`tests/test_v06_module_size_gate.py::GRANDFATHERED` 는 **빈 딕셔너리**
입니다 (실행으로 확인: `len == 0`). vision-wire PR 이후 처음으로 예외가
하나도 없는 상태입니다. `core/` 최대 파일은
`core/change_request.py` 20,338 B < 20,480 B.

### 4.5 rule #1 (도메인 분화 금지) — 위반 0건

`core/**/*.py` 를 버티컬 토큰으로 훑으면 13건이 잡히는데, **전부**
경계를 *선언하는* 주석이거나 일반 영어입니다:

- `core/ontology.py:18` — "no legal/food/retail/finance-specific types"
- `core/relations_schema.py:90` — "boundary test (no legal/food/finance vertical drift)"
- `core/workspace.py:12` — 'not "legal-review" / "food-spec"'
- `core/plugins/base.py:158` — "a legal return value" (일반 영어)
- `core/lifecycle/retention.py` — "legal retention windows" (엔터프라이즈 일반 개념)

도메인 기능은 **0건**. 템플릿 엔진(#909)도 rule #1 을 지켜 템플릿을
하나도 동봉하지 않습니다.

### 4.6 CI

| 항목 | 값 |
|---|---|
| main `5307ab3` | `tests` / `lint` / `security-scan` 전부 success |
| 테스트 | **5,309 passed / 0 failed / 8 skipped** |
| `--ignore` 목록 | **0** (#1111 이 51건 전부 회수) |
| CSP `style-src` | `'self' https://fonts.googleapis.com` — `'unsafe-inline'` 없음 |

---

## 5. 이 마감이 **하지 않은** 것

의도적으로 남겨 둔 것들입니다. 다음 세션이 "빠뜨렸다"고 오해하지
않도록 적습니다.

1. **발표 수치는 건드리지 않았습니다.** preprint README / `.zenodo.json`
   / `docs/release_notes_v0.4.4.md` 의 LRB S2 값은 여전히 수리 전
   **0.225 / 0.5375 / 0.7125** 입니다. #1089 이후 저장소가 재현하는 값은
   **0.2500 / 0.5875 / 0.7625** 이고 **J − N 은 0.175 로 동일**합니다.
   재베이스라인 여부 = **LRB 결정 #2, operator 판단** (2026-09-08 마감
   §7.1 부터 미결). 이번 릴리스 노트는 이 괴리를 *기록*만 하고 수치를
   고치지 않습니다.
2. **Zenodo DOI 미발행** — 로드맵 §Phase 4 가 명시한 대로 operator 판단.
3. **`JAMES_CSP_MODE` enforce 플립 안 함** — 남은 블로커는 `graph.html`
   의 `unpkg.com` 스크립트 3종 (`script-src 'self'` 가 차단).
4. **`v0.5` 소급 태그 안 함** — §2 의 계보 문제, operator 판단.
5. **v0.6 정식 진입 선언 안 함** — Fork A/B 는 operator 전략 결정
   (판정 시점 ≈ 2026-12-13). 태그 `v0.6.1` 은 *제품 릴리스*이지 사이클
   진입이 아닙니다.

---

## 6. operator 결정 대기 (우선순위)

| # | 항목 | 왜 세션이 못 하나 |
|---|---|---|
| 1 | **LRB 결정 #2** — 발표 수치 재베이스라인 vs 각주 | 발행된 DOI 와 preprint 에 영향 |
| 2 | **`v0.6.1` Zenodo DOI 발행** | 전략 결정 종속 (§Phase 4 규약) |
| 3 | **`v0.5` 소급 태그** | DOI 계보 변경 |
| 4 | **`unpkg.com` 3종 vendoring** → enforce 플립 | 외부 의존성 추가 |
| 5 | **dogfood 4건** (3.1 긴 질의 / 3.2 detailed / #1084 캡 / #1091 한글 파일명 OCR) | 실기기 필요 |
| 6 | **Fork A/B** | 전략 |
| 7 | **S3.2 project-lead 제목 충돌 수정** (`build_lrb_scenario_s3.py::make_project` 생성기) — 2026-09-10 v0.2.3b publication 리포트 §4 자기 catch | 발표된 S3 token-mode 0.845 (README / SUMMARY / preprint §4.6) 가 바뀝니다 — 결정 #1 과 같은 성격. **✅ 2026-09-12 operator 승인 · 실행** — `docs/research/lrb-v023-s3-publication-scale-results-2026-09-12-s32.md` (publication token V/N/J 0.566 / 0.827 / 0.984, J−N +0.157; README / SUMMARY / preprint §4.6 재베이스라인) |

---

## 7. 남은 솔로 작업 (로드맵 Phase 5 이전)

1. ~~**`mobile.css` 잔여 `!important` 20개**~~ — **✅ #1116 (2026-09-10)
   20 → 0.** 모바일 폭(375/480/768) 계산 스타일 대조 36 대상 × 3 뷰포트
   = 108 비교 불일치 0, 해석 여부 별도 확인 (375/768 에서 모바일 값 적용,
   1280 에서 데스크톱 값 복귀). 캡 테스트는 "≤ 25" → **선언 0건**, 주입
   프로브로 실패 가능함을 확인. `mobile.css` 는 이제 헤더의 약속대로
   **캐스케이드 위치만으로** 이깁니다.

   → **서버 없이 세션 혼자 할 수 있는 큐는 여기서 소진**됐습니다. 아래
   둘은 LLM 실행이 필요한 **operator-attended** 항목입니다.

2. ~~**로드맵 Phase 5 측정 백로그**~~ — **✅ 3건 전부 실행 (2026-09-10 →
   09-11)**. 실행 규칙 (PYTHONPATH / nohup 금지 / timeout 금지 / 서버
   background 금지) 은 로드맵 §Phase 5 그대로 지켰습니다.
   - Graph-RAG Step 2 cross-model — #1118 (집계기) / #1119 (Table 3.2),
     5 h 24 min. `reports/research-runs/graph-rag-step2-cross-model-20260910.md`.
   - D-alce cross-NLI 양 leg — #1120. DeBERTa − RoBERTa Δ > 0.10 →
     prereg 의 cross-NLI disagreement 행 발동.
     `reports/research-runs/alce-d-cross-nli-smoke-20260910.md`.
   - v0.2.3b LLM-grounded **S3 publication** 3-model — 9 셀 × n=1000,
     20 h 24 min, EXIT=0. **V<N<J 3/3, J−N +0.127 / +0.134 / +0.142
     (전부 > +0.10), james temporal accuracy 0.994–1.000.** claude cloud
     leg 는 미실행 (cloud backend 필요 — 6월 smoke 와 같은 제외) 이므로
     prereg 의 4/4 행은 주장하지 않습니다.
     `reports/research-runs/lrb-v023b-3model-llm-grounded-publication-20260910.md`.
     🔴 **그 리포트 §4 자기 catch**: S3 publication fixture 의 project-lead
     질의 191건 (19.1 %) 이 제목 충돌 (`make_project` 의 period-20 산술 →
     100 부서에서 제목 50개가 5× 반복, 질의에 부서 없음) 때문에 **어떤
     SUT 도 R@1 0.288 을 넘을 수 없는** 상한을 안고 있습니다 — 발표된
     token-mode 0.845 도 같은 fixture 라 같은 상한입니다. 판정에는
     보수적으로 작용합니다 (N 과 J 를 같은 값으로 누름). 생성기 수정
     (S3.2) 은 발표 수치를 바꾸므로 §6 #7 — **2026-09-12 실행됨**.
   → Phase 5 잔여 = claude cloud leg (cloud backend, operator) + 아래 3.
3. **QVT 3축** — Graded Answer / Abstention F1 / Token Cost 미측정,
   arm 당 ~70분, 디스크 baseline 은 `2a31b20`(5월)뿐이라 재캡처 필요.
   위와 같은 이유로 operator-attended.

---

## 8. 마감 검증

| 검사 | 결과 |
|---|---|
| `git rev-list --count v0.4.4..main` | 282 |
| `core/retrieval` + `core/retrieval_engine.py` diff | 0 라인 |
| `GRANDFATHERED` 항목 수 | 0 (import 해서 확인) |
| rule #1 버티컬 토큰 | 13건 전부 경계 선언 / 일반 영어 |
| `tests/` (로컬) | 5,315 passed / 0 failed |
| main CI (`5307ab3`) | tests / lint / security-scan 전부 success |
