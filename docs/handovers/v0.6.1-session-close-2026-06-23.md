# v0.6.1 — Session close / next-session entry (2026-06-23)

> 🟢 READ FIRST. This session ran long (PRs #992–#1037). Below is the
> resumable state + the work queue. main = `516673e`, tree clean, no
> background jobs running.

## What this session shipped (themes)
1. **Features**: vision mode handler + engine wire (#991); workspace
   reasoning-edit modal (#992).
2. **UI consolidation** (frontend pages 8 → 5):
   - de-emoji of all non-chat chrome → SVG/text (#998–#1003).
   - **intro front door**: onboarding + glossary merged into `/`; chat
     moved to `/chat`; old routes 301 (#1004–#1007). + scroll-reveal
     animation (#1029).
   - **graph hub**: reasoning-flow + knowledge-rollback folded into
     `/admin/graph` as tabs; old routes 301 (#1008–#1011).
   - **workspace 원본자료 tab**: the dead 검색 placeholder became a real
     find→view→edit surface over `/admin/files/*` + wiki-edit (#1017).
3. **Trace linkage loop** (answer→trace→graph): answer "추론 보기"
   deep-link (#1014), trace search by question (#1015), flow↔graph
   cross-link (#1016).
4. **Entity-edit lifecycle cascade** (editing a wiki entity keeps the
   graph consistent): Phase 1 invalidate stale (#1018), Phase 2 add new
   (#1019), Phase 3 inbound-inverse + T6 (#1033); result surfaced in the
   edit UI (#1034). core/cascade only.
5. **🔴 Lifecycle live-consistency arc** (the big measured finding): the
   probe (#1020) proved live graph traversal IGNORED lifecycle status
   (cascade was COSMETIC at the live layer — honored only in time-travel).
   Fixed across every user-facing surface: traversal output (#1021),
   graph score (#1023), T1 validity window (#1024), 3D snapshot (#1026);
   time-travel isolation verified+pinned (#1025); audit clean + arc closed
   (#1027). `relation_is_live()` in core/graph_engine/constants.py is the
   gate; kill-switch `JAMES_DISABLE_STATUS_FILTER`. Report:
   `reports/research-runs/lifecycle-live-consistency-arc-20260622.md`.
6. **Visual-regression harness** (#1013, + reduced-motion #1035): real
   headless-Chromium render of 7 pages, console-error + pixel-diff vs
   `reports/visual/baseline/`. **playwright+chromium+Pillow already
   installed.** Deterministic (reduced-motion). The session's UI work was
   harness-verified.
7. **LLM backlog re-measure** (operator-requested, #1028/#1030/#1031/#1032):
   D-alce (sampling variance) · HR N=100 (IDENTICAL to baseline) ·
   v0.2.3b smoke (J−N +0.21 reproduced). ⇒ the lifecycle filter caused
   **no measurable regression** on the supersede-sensitive LRB SUTs.
8. **UX/UI consistency review** (#1035–#1037): chrome/links/theme/i18n
   coherent; fixed i18n gaps + harness flakiness; **unified header buttons
   → shared `.nav-btn` in tokens.css** (#1036, + fixed intro's latent
   unstyled header buttons) + **unified primary-CTA identity** (glow +
   accent-hover across .btn-primary/.onb-btn-primary/.modal-btn.primary,
   #1037).

## Invariants held all session
- `core/retrieval` + `core/graph` **traversal** untouched EXCEPT the
  measurement-gated lifecycle filter (#1021/#1023/#1024/#1026 — the one
  sanctioned streak break, probe + structural Δ=0 evidence). `core/reasoning`
  only the earlier engine mode-routing. No vertical/domain code. core/ <20KB.
- Every PR: branch → squash-merge → sync main. No direct-to-main.

## Work queue for next session
### Solo-doable (code)
- **CSP style-src enforce** — blocked on migrating ~573 inline `style=`
  attrs → classes (nonce does NOT apply to attributes). NUANCED: many are
  JS-toggled (display:none etc.) so not a pure mechanical move. script-src
  is already strict. Large, incremental, harness-verify each page.
- **Button taxonomy — naming** (low ROI): class names still differ
  (btn-primary/onb-btn-primary/modal-btn.primary); visual identity already
  unified. A full `.btn--primary` base+modifier system = large migration.
- Minor: de-emoji content residue (📄 in answer text — mostly KEEP).

### Operator-gated (decision/action)
- **★ v0.6 entry decision** — Fork A (LOI signed → Track D vertical pack +
  G8.d) / Fork B (6mo no-LOI → reassess). The biggest lever. See
  `docs/handovers/v0.6-entry-skeleton-2026-06-13.md`.
- **Live LLM cascade dogfood** — edit a real entity in the workspace
  원본자료/추론편집, watch "관계 N개 무효화/추가/역방향", then query to
  confirm the live answer drops the stale relation (KG-mutating + mixtral
  non-deterministic → not an autonomous PR).
- Full v0.2.3b 3-model matrix (hours, low marginal info); G8.d / G2 OIDC /
  vertical pack (LOI); #448 gemma4 26b collab research.

### Out of scope / future
- entity-edit cascade: 2000-char extract truncation; inverse-label
  fallback over-invalidation on multi-relation pairs; T6 only for id'd edges.

## Operator quick-refs
- visual harness: `python scripts/visual_regression.py [--update-baseline | --token <jwt>]` (server must be up).
- cascade probe (deterministic, no server): `python scripts/research/cascade_consistency_probe.py`.
- ⚠ Background measurement: use `PYTHONPATH=<repo-root>` + **no `nohup`**
  (nohup detaches python → harness never notifies) + **don't wrap LLM runs
  in your own `timeout`** (it killed a v0.2.3b run, EXIT 124).
- New memories this session: project_{deemoji_series,intro_front_door,
  graph_hub_consolidation,visual_regression_harness,workspace_audit_source_docs,
  trace_linkage_loop,entity_edit_cascade}_v0_6_1 + d_alce re-measure note.
