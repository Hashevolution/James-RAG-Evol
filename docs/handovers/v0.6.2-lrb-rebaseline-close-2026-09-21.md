# v0.6.2 — LRB 재베이스라인 arc 마감 (#1121–#1128)

> **다음 세션 진입점.** rule #6 에 따라 사이클 상태의 원본은 이 문서입니다.
> Phase 정의 원본은 `v0.6.2-restart-roadmap-2026-09-03.md` §2,
> 사이클 마감 실측은 `v0.6.1-close-2026-09-10.md` 가 계속 유효하고,
> 이 문서는 그 **§6 결정 #1 / #7 을 실행한 결과**와 그 뒤에 남은 것을
> 담습니다.
>
> **측정 구간**: 2026-09-12 → 09-14 (PR #1121–#1128)
> **작성**: 2026-09-21 — 측정 종료 **7일 뒤**. 그 지연 자체가 §2 입니다.
> **CI**: `tests` / `lint` / `security-scan` 전부 success
> (`0ad04b9`, run 34774021077 · 4m10s)

---

## 1. 한 줄 요약

`v0.6.1-close-2026-09-10.md` 가 operator 판단으로 남겨 둔 **LRB 결정
#1 · #7 (발표 수치 재베이스라인)** 을 2026-09-12 에 승인받아 실행했고,
그 과정에서 **측정 위생 결함 3건**을 잡아 재실행한 뒤, S3 publication
4-leg cross-model 을 **사전등록된 판정표 그대로 ⭐⭐ partial (3/4)** 로
닫았습니다. 발표 수치가 바뀌었고, **JAMES 주장은 바뀌지 않았습니다.**

---

## 2. 이 문서가 7일 늦은 이유 — rule #6 이 실제로 실패한 지점

기록해 둡니다. 다음 세션이 같은 실패를 반복하지 않도록.

#1121–#1128 은 **자체 핸드오버 없이** 머지됐습니다. 그동안 상태 서술은
두 군데로 흘러갔습니다.

1. `CLAUDE.md` 의 "Where we are right now" 불릿 — 09-12 / 09-13 / 09-14
   의 측정 수치와 PR 번호가 본문에 직접 쌓였습니다.
2. `v0.6.1-close-2026-09-10.md` §7 의 마지막 불릿 — 한 문단이 계속
   덧붙어 **한 줄 600자를 넘는 각주**가 됐습니다 (그 문서가 담기로 한
   것은 09-10 시점 마감인데, 09-14 까지의 진행이 끼어 들어갔습니다).

**가드는 이것을 잡지 못합니다.** `test_v06_claude_md_entry_pointer` 의
불변식 #4 는 *"CLAUDE.md 첫 행이 디스크에서 파일명 날짜가 가장 최신인
핸드오버를 가리키는가"* 를 검사합니다. 새 핸드오버를 **안 만들면**
가장 최신은 여전히 09-10 이고, 포인터는 거기를 가리키고 있으니
**초록입니다**. 가드는 "포인터가 낡는 것"은 막지만 "핸드오버를 아예
안 쓰는 것"은 막지 못합니다 — rule #6 이 금지한 두 번째 행동이 정확히
후자입니다.

이번 PR 은 그 상태를 이 문서로 끌어내고 두 원본을 포인터로 되돌립니다.
가드 강화는 하지 않습니다: "마지막 커밋 날짜 대비 핸드오버가 N일
이상 낡으면 실패" 같은 규칙은 문서 전용 PR 과 유휴 구간을 구분하지
못해 거짓 적색을 만듭니다. 대신 이 §2 를 남겨 다음 세션이 **측정 arc
를 닫을 때 핸드오버를 함께 쓰도록** 합니다.

---

## 3. PR 표 (#1121–#1128)

| PR | 날짜 | 성격 | 내용 |
|---|---|---|---|
| #1121 | 09-12 | docs | v0.2.3b LLM-grounded S3 publication 3-model 결과 + **§4 자기 catch** (project-lead 천장) |
| **#1122** | 09-12 | fix | **S3.2** — `build_lrb_scenario_s3.py::make_project` 제목 충돌 수리 + S3 발표 수치 재베이스라인 + 생성기 가드 |
| **#1123** | 09-12 | fix | reranker **fallback 계수** — 질의별/셀별 조용한 토큰 순서 낙하를 세어 `result.json` 에 기록 |
| **#1124** | 09-12 | fix | claude reranker — repo 밖 cwd (CLAUDE.md 미적재) + 짧은 system prompt + UTF-8 + 세션 파일 없음 |
| #1125 | 09-12 | docs | **결정 #2 실행** — S2 를 수리 fixture 에서 재베이스라인, 4-model leg 재측정 |
| **#1126** | 09-12 | fix | claude reranker — 쿼터 소진 시 **fallback 대신 대기·재시도** |
| #1127 | 09-13 | docs | S3.2 fixture 에서 로컬 3-model 재측정 (9,000 행 **fallback 0**) |
| #1128 | 09-14 | docs | **claude leg** + 4-leg composite + preprint §4.7 |

**`core/` 0 라인.** `git diff --stat v0.6.1..main -- core/` = 빈 출력.
이 arc 는 전부 측정 측 (`eval/external/lrb/`, `scripts/research/`,
`tests/`, 문서) 입니다 — rule #2 의 벤치 게이트 대상이 아닙니다.
92 파일 / +9,364 / −168 (대부분 커밋된 `result.json`).

---

## 4. 무엇이 측정됐나

### 4.1 S3.2 — 생성기 결함 수리 (#1122)

#1121 §4 의 **자기 catch**: S3 publication fixture 의 project-lead
질의 191건 (19.1 %) 이 `make_project` 의 period-20 산술 탓에 제목
충돌을 안고 있어 **어떤 SUT 도 R@1 0.288 을 넘을 수 없는 천장**이
있었습니다. 발표된 token-mode 0.845 도 같은 fixture 였습니다.

수리 후 fixture sha `cc98e141…` — **330/330 고유 프로젝트 제목,
모호한 질의 텍스트 0**. 생성기 가드는 `tests/test_lrb_s3_generator.py`
(+82 라인).

| S3 publication, token mode | V | N | **J** | J − N |
|---|---|---|---|---|
| 발표값 (2026-06-12, 수리 전) | 0.502 | 0.721 | 0.845 | +0.124 |
| **S3.2 (2026-09-12)** | **0.566** | **0.827** | **0.984** | **+0.157** |

천장은 **N 과 J 를 같은 값으로 누르고 있었으므로** 판정에 보수적으로
작용했습니다 — 수리는 gap 을 줄이지 않고 **키웠습니다**.

### 4.2 S2 — 수리 fixture 재베이스라인 (#1125)

결정 #2. #1089 이 S2 생성기의 정책 제목 충돌을 수리한 뒤 저장소가
재현하는 값과 발표값이 갈라져 있었습니다. **token 셀만 고치면 논문의
cross-model 표가 저장소가 재빌드할 수 없는 fixture 위에 남으므로**,
4-model LLM-grounded 도 수리 fixture (sha `2c1b210c…`) 에서 재측정.

| S2 R@1 | token | gemma4:e4b | gemma3:12b | mixtral:8x7b | claude |
|---|---|---|---|---|---|
| vanilla | 0.2500 | 0.4750 | 0.4500 | 0.3625 | 0.5250 |
| naive-supersede | 0.5875 | 0.7375 | 0.7250 | 0.6875 | 0.7750 |
| **JAMES** | **0.7625** | **0.9000** | **0.9000** | **0.8625** | **1.0000** |
| V<N<J | ✓ | ✓ | ✓ | ✓ | ✓ |
| **J − N** | **+0.1750** | +0.1625 | +0.1750 | +0.1750 | +0.2250 |

**5/5 leg 전부 V<N<J**, J−N 은 발표값 대비 ±0.04 이내 (80 질의 중
±3건). 절대 JAMES R@1 은 +0.025 … +0.175 올랐습니다 — 수리된 fixture
에서는 같은 숫자를 가진 낡은 정책 문서가 1위를 뺏지 못하기 때문입니다.

### 4.3 S3 publication 4-leg composite (#1127 / #1128)

12,000 LLM-grounded 행 (로컬 9,000 + claude 3,000), **fallback 0**.

| S3 pub R@1 | token | gemma4:e4b | gemma3:12b | mixtral:8x7b | claude-haiku-4-5 |
|---|---|---|---|---|---|
| vanilla | 0.566 | 0.554 | 0.634 | 0.550 | **0.852** |
| naive-supersede | 0.827 | 0.819 | 0.813 | 0.754 | 0.833 |
| **JAMES** | **0.984** | **0.989** | **0.991** | **0.893** | **0.990** |
| V<N<J | ✓ | ✓ | ✓ | ✓ | **✗ (V > N 0.019)** |
| **J − N** | +0.157 | +0.170 | +0.178 | +0.139 | +0.157 |

**사전등록 판정표 그대로 적용: 3/4 leg clear → ⭐⭐ partial-tier
cross-model + 1건 attribution finding.** 4/4 PUBLICATION-TIER 는
주장하지 않습니다. 보조 조건 (J−N > +0.10) 은 **4/4 충족**.

🔴 **깨진 것이 무엇인지 정확히**: 깨진 것은 **두 베이스라인의 순서
(V vs N)** 이지 JAMES 기여가 아닙니다. J > N, J > V 는 10개 LLM-grounded
셀 전부에서 성립하고 J−N 은 +0.139 ~ +0.225 입니다.

**원인 (fixture 텍스트 × reader 강도)**: S3 의 모든 대체 문서는 본문에
`Supersedes <old id>` 를 적습니다 (260/260 SUPERSEDE 이벤트). **S2 는
0/48**. 강한 reader 는 그 문장을 읽고 vanilla 가 보유한 낡은 판본들
사이에서 현재 판본을 1위로 올립니다 — claude 에서 vanilla 의 현재
상태 카테고리가 1.00/1.00/1.00 (로컬 reranker 는 0.00–0.46). Naive 는
이미 히스토리를 지워서 이득을 볼 수 없습니다 (R@1 = R@10 = 0.833).
S2 에서는 그 문장이 없어 claude 도 V<N 을 유지합니다 (0.525/0.775/1.000).

**크기**: V − N = 0.019 = 1000 질의 중 19건, reranker ≈10 pp 밴드
(#819) 안 — "뒤집혔다"가 아니라 **구분 불가**입니다.

**하지 않은 것**: 대체 문장을 뺀 S3.3 변종이면 reader 강도와 fixture
문구를 분리할 수 있지만, 벤치마크 측 변경이므로 preprint §7 future
work 로만 올렸습니다.

---

## 5. 측정 위생 3건 — 이 arc 의 진짜 교훈

두 번의 claude leg 실행을 **폐기**했습니다. 폐기 판단을 가능하게 한 것이
#1123 입니다.

| # | 결함 | 증상 | 수리 |
|---|---|---|---|
| **#1123** | reranker 가 실패 시 **조용히** 토큰 순서로 낙하 | 763/1000 · 876/1000 · 281/1000 행이 사실상 미측정인데 결과는 정상으로 보임 | 질의별/셀별 fallback 계수 → `result.json` 에 `rerank_fallbacks` |
| **#1124** | `claude -p` 가 repo 안에서 돌아 **CLAUDE.md 가 호출마다 적재** | Max 플랜 쿼터를 태워 셀 중간에 소진 | repo 밖 cwd + 짧은 `--system-prompt` + UTF-8 + 세션 파일 없음 |
| **#1126** | 쿼터 소진 = fallback | 위와 결합해 오염 재발 | **대기 후 재시도** (claude leg 에서 11 × 10분 = 6,600 s 대기, fallback 0) |

**#1123 이 없었으면 오염된 실행이 결과로 발표됐을 것입니다.** 이 프로젝트의
9건 catch 가 만든 "측정을 코드에 맞추지 말라"는 룰과 같은 계열이고,
메모리 `feedback_headless_claude_cli_measurement_hygiene` 에 박혔습니다.

⚠️ **cloud leg 는 세션과 Max 쿼터를 공유합니다.** claude 셀은 ≈3,000 CLI
호출 × ≈20 s + 1.8 h 대기입니다. 다음 cloud leg 를 돌릴 때 같은 창에서
작업 세션을 쓰면 또 소진됩니다.

---

## 6. 발표 수치 — 바뀐 것과 안 바뀐 것

**바뀐 것** (재베이스라인 완료):
- `README.md` / `README.ko.md` / `SUMMARY.md` — S3 publication 수치
- preprint `papers/lrb-preprint/main.tex` §4.6 (S3) · §4 표 3종 (S2) ·
  **§4.7 신설** (cross-scale × cross-model 10셀 + attribution finding)
- `docs/research/lrb-v023-s3-publication-scale-results-2026-09-12-s32.md`
- pre-LOI 자료 (`reports/promo-assets/v0.5-pre-loi-materials/`)
- `benchmarks/README.md` · `docs/evaluation/` 3종

**의도적으로 안 바꾼 것**:
- **`.zenodo.json`** — v0.4.4 예치 **원문 그대로**. 예치된 기록물은
  사후 편집 대상이 아닙니다.
- **`docs/release_notes_v0.4.4.md`** — **erratum 줄만** 추가.
- **v0.4.4 DOI** `10.5281/zenodo.20652679` 는 그대로가 최신 DOI입니다.

---

## 7. 불변식 — 이 arc 종료 시점 실측

| 검사 | 명령 | 결과 |
|---|---|---|
| `core/` 전체 | `git diff --stat v0.6.1..main -- core/` | **0 라인** |
| `core/retrieval` | 같은 명령, 경로 한정 | **0 라인** |
| rule #5 게이트 | `python -m unittest tests.test_v06_module_size_gate` | OK, `GRANDFATHERED` **0건** |
| 엔트리 포인터 가드 | `tests.test_v06_claude_md_entry_pointer` | OK (이 PR 이 첫 행을 이 문서로 이동) |
| main CI | run 34774021077 (`0ad04b9`) | tests / lint / security-scan **전부 success** |

`core/graph` traversal 의 의도된 streak 예외 (lifecycle live-consistency
arc, #1021/#1023/#1024/#1026) 는 v0.6.1 마감 문서 §4.2 의 기록 그대로이며
이 arc 에서 추가 변경은 없습니다.

---

## 8. 남은 일

### 8.1 세션 혼자 가능 — **소진됨 (2026-09-21)**

서버·LLM 없이 세션 혼자 할 수 있는 큐는 이 세션에서 비웠습니다.
남은 것은 전부 operator 입력이 필요합니다 (§8.3).


| # | 항목 | 비고 |
|---|---|---|
| 1 | ~~핸드오버 공백~~ | **✅ 이 문서** |
| 2 | ~~`papers/lrb-preprint/README.md` 상태 낡음~~ | **✅ #1131.** 상태 헤더 재작성 + §4/§5/§6 체크. **그리고 §3 이 실제로 미완인 것을 발견** — 초록은 "three scenarios" 인데 §3 은 "two", Introduction 기여 목록도 "Two pre-registered scenarios" 였습니다. S3 는 §5.7/§5.8/§4.7 에서 측정·논증되는데 **사양이 어디에도 없었습니다** → **✅ #1132** 가 §3 에 S3 작성 (3 preset · 10 카테고리 · S3.2 수리 · `Supersedes` 문장). `% TODO` **1 → 0** |
| 3 | ~~honesty checklist **Item 2**~~ | **✅ #1131.** 인용 **8/8 전부 접근 가능** (대조군 `arXiv:2699.99999` → 404 로 검사기가 실제로 판별함을 확인). 접근성 문제는 0, 대신 **메타데이터 결함 5건**: 🔴 `chu2024timebench` 가 **논문에 없는 저자 3명**을 등재 (10명 → 7명, ACL + arXiv 2311.17667 **직접 확인** 후 제거) · 단독저자에 `and others` · 저널 논문에 `@inproceedings` · 책에 `@article` (출판사가 `journal` 필드) · DOI 4건 누락. 체크리스트 자체도 **존재하지 않는 키** `reflow2026dfah` 를 지목 (실제 `khatchadourian2026dfah`) |
| 4 | ~~유령 PR 2건~~ | **✅ 둘 다 머지 (2026-09-21).** [#687](https://github.com/Hashevolution/James-RAG-Evol/pull/687) — Track 2c Phase 4 분석 (신규 파일 2개, **+889, 순수 추가**). main 이 3곳에서 참조 중이었고 한 핸드오버는 "✅ PR #687" 로 **완료 표시**까지 해 둔 상태였습니다. Phase 5 는 main 에 있는데 그게 기대는 Phase 4 분석만 없었습니다. [#375](https://github.com/Hashevolution/James-RAG-Evol/pull/375) — promo 기록 3행 + placeholder 보충. 🔴 **정정**: 이 표의 이전 판은 *"launch-tracker.md 에는 velog URL 이 있다"* 고 적었는데 **틀렸습니다**. 거기 있던 것은 **K4** 행의 괄호 문구 `(… full at velog.io/@hashevolution)` 였고, **K5 velog · X K6 · Reddit E1 세 행은 main 에 전혀 없었습니다** (세 식별자 전수 검색 0건). 즉 절반 착륙이 아니라 **미착륙**이었고, 머지는 중복이 아니라 누락 보충입니다 |
| 5 | ~~참조 부패 조사~~ | **✅ #1133** + #687 머지. 88 참조 중 10 dangling → **8** (#687 이 2건 해소). CLAUDE.md 표의 죽은 포인터 2건 (bidi 감사 / 존재하지 않는 `scripts/step7_query_test.py`) 을 명시. 7건은 **어디에도 커밋된 적 없음** → 새로 쓰면 복구가 아니라 창작이라 기록만. 상세 §8.2 |

### 8.2 참조 부패 실측 (2026-09-21 전수 조사)

`docs/` · `reports/` · `CLAUDE.md` 가 참조하는 `reports/research-runs/*`
경로 **88개**를 전부 확인했습니다 — **10개가 main 에 없었습니다** (#687 머지 후 **8개**).

| 부류 | 수 | 파일 | 판단 |
|---|---:|---|---|
| ~~열린 PR 안에 있음~~ **✅ 머지됨** | 2 | `track-2c-phase-4-jam-m_m-analysis-20260602.md` · `adversarial-sweep-M_M-20260602T084119.json` | **PR #687** — 2026-09-21 머지, 해소됨. 남은 dangling = **8** |
| **머지 안 된 브랜치에 있음** | 1 | `bidi-normalization-audit-20260602.md` | `docs/v0.4-track-2c-ali-integration` (`c317dda`). 🔴 **`docs/ARCHITECTURE.md` §5.7.12 와 CLAUDE.md 표가 참조**. 이 PR 이 양쪽에 "main 에 없음" 을 명시 |
| **어디에도 커밋된 적 없음** | 7 | `alpha-6-phase-3a-gemma3-{1b,12b,27b}-analysis-20260602.md` · `architecture-md-5-7-12-typed-filter-draft.md` · `qvt-ablation-T0-smoke-analysis.md` · `v3prime-{reflect,verify}-*.json` | `git log --all` 에 이력 0. 핸드오버가 **커밋한 적 없는 파일을 인용**했습니다 |

⚠️ **마지막 부류를 "복구" 할 방법은 없습니다.** 파일이 존재한 적이
없으므로 내용을 새로 쓰면 그건 복구가 아니라 **창작**입니다. 인용하는
핸드오버는 2026-06-01 / 06-02 의 봉인된 문서이므로 이 PR 은 건드리지
않았습니다 — 과거 문서를 소급 수정하는 것과 "참조가 깨졌다"를 기록하는
것은 다른 일이고, 지금 필요한 건 후자입니다.

> `bidi-normalization-audit` 건은 **코드가 없다는 뜻이 아닙니다.**
> `core/input_normalization.py` 와 `tests/test_input_normalization.py`
> (43 tests, green) 는 main 에 있습니다. 착륙하지 않은 것은 *감사
> 보고서* 쪽입니다.

### 8.3 operator 필요

| # | 항목 | 왜 막혀 있나 |
|---|---|---|
| 1 | **QVT** — ✅ A (5축 베이스라인) 완료 · B (T0 smoke) 는 2026-09-22 에 57% 생성 실패로 중단 → **러너 수리 완료 (#1149, 2026-09-24)**, 재실행 가능 — 서버 승인 필요, ~2.2 h → §8.5. 지연 분해 arc (#1145–#1147) → **§8.6** |
| 2 | **dogfood 4건** — 3.1 모바일 긴 질의 / 3.2 detailed / #1084 캡 / #1091 한글 파일명 OCR | 실기기 |
| 3 | **`unpkg.com` 3종 vendoring** → CSP `enforce` 플립 | `frontend/graph.html:367-369` (three 0.160.0 / 3d-force-graph 1.73.4 / d3-force-3d 3.0.5). 현재 `JAMES_CSP_MODE` 기본 `report-only` (`core/security/headers.py:107`). 로컬 우선 원칙상 vendoring 이 답 |
| 4 | **결정 #8 — LRB 제품 전체 셀** | 실서버 + API 로 5,620 이벤트 재생 + `/query/` 시점 매개변수 = `core/retrieval` 변경 (rule #2). 어댑터 JAMES (S2 0.7625) 보다 낮으면 배선 결함. S2 ≈ 2 h / S3 ≈ 1 일 |
| 5 | **`v0.6.1` Zenodo DOI 발행** | 전략 결정 종속 |
| 6 | **`v0.5` 소급 태그** | DOI 계보 변경 |
| 7 | **arXiv 제출** | Item 3 (commit hash 를 `\thanks{}` 에) + Item 4 (LRB 아티팩트를 `10.5281/zenodo.20625533` 에 번들 vs 별도 DOI) = operator 결정 |
| 8 | **TimeQA / TempReason** | 데이터 다운로드 + 라이선스 확인 |
| 9 | **Fork A/B** | 전략. 판정 ≈ **2026-12-13** (약 3개월 남음) |

---

### 8.4 QVT 진입 준비 (2026-09-22) — 측정 전에 잡은 하네스 결함 4건

🔴 **이전 판의 "QVT 3축 (Graded Answer / Abstention F1 / Token Cost) 미측정" 은
부정확했습니다.** 그 3축은 2026-05 베이스라인에 **이미 있습니다**
(path 1.0 / graded 0.5833 / abstention 0.6667, n=3). 없는 것은 **5축 정본
베이스라인** 과 매트릭스 셀 28/30 입니다. rule #2 가 "α-5 Pareto 판정은 5축
베이스라인이 정본이 된 뒤 적용" 이라고 적은 그 조건이 아직 충족되지 않은 상태입니다.

**실측한 환경** (전부 서버 없이 확인):

| 항목 | 결과 |
|---|---|
| `:8000` | 비어 있음 (운영자 서버 없음) |
| Ollama | UP, 매트릭스 모델 5종 (1b/4b/e4b/12b/27b) 전부 설치 |
| pre-flight | **PASS** (0 warnings) |
| bench 스위트 | `step7-v7`, **20 쿼리** 적재 확인 (`bench.py --dry-run`) |
| JWT 발급 | OK (len 203) |
| 캡처 dry-run | 출력 = `baseline_721e109.json` (**기존 파일 덮어쓰지 않음**) |
| 전체 매트릭스 | **30 셀 ~33 h**. 설계된 게이트 = **T0 smoke** (L1,L5 × M_M) **~2.2 h** |

**잡은 결함 4건** — 전부 같은 모양입니다: *무언가 일어나지 않았는데 아티팩트는
깨끗해 보였다.* #1123 (LRB fallback 계수) 의 계열이고, 이걸로 다섯 번째입니다.

| PR | 결함 | 왜 중요한가 |
|---|---|---|
| **#1136** | 셀 JSON 이 **AUTO_ROUTER 가 라우팅할 곳이 있었는지** 기록 안 함 | 레지스트리가 `small: ['ollama_local']` 뿐이고 **medium/large/cloud 전부 비어 있음**. L2·L5 가 `AUTO_ROUTER=1` 이므로 rule #2 의 *"not in evidence" ≠ "no effect"* 조건에 정확히 해당. α-5 에서 **이미 한 번 겪은** 패턴 (`feedback_oracle_phrase_artifacts`). 부수 수정: `--help` 가 cp949 콘솔에서 옵션 한 줄도 못 찍고 죽던 것 (UTF-8, #1124 와 동일) |
| **#1137** | 비교되지 않은 **비용 축이 `Δ 0.0` = flat** 으로 채점됨 | 디스크 베이스라인이 3축이라 `token_cost`/`latency_cost` 가 없음 → `base_med=None` → delta 0.0 → cost flat → **`quality+ & cost flat = adopt`**. 재현 확인: 토큰 비용 2배인 셀도 `adopt`. 이제 `n/a` + 판정 기권 (`quality-positive (cost not in baseline)`) |
| **#1138** | **교차-fixture 델타**가 레이어 측정으로 제시됨 | 베이스라인 `step7-v5` (05-28) vs 커밋된 셀 2개 `step7-v7` (06-03). step7 은 v5→v7 에서 쿼리가 **추가**됐습니다. `fixture_version` 은 스키마 생긴 이래 **모든 셀에 기록되고 한 번도 읽히지 않았습니다**. 그 델타에 Pareto 판정이 붙어 있었고 (`L1/M_CLOUD` = `reject`), 이제 `not comparable` 로 **대체** |
| **#1139** | `_read_baseline` 이 **파일명 사전순 마지막**을 "최신" 으로 선택 | git sha 는 16진수 — 사전순과 시간순 무관. 이번 재캡처(`721e109`)는 **우연히** 맞지만, 이후 더 낮은 sha 로 캡처하면 새 베이스라인이 **영원히 무시**됩니다. `captured_at` 기준으로 교체 + 선택 근거(파일/시각/신호/schema/fixture) 출력 |

**재캡처가 왜 선행인가** — 한 번에 세 가지를 해결하기 때문입니다:

1. `qvt-baseline-v2` = **5축 + per-question-type** 생성 (rule #2 가 기다리던 정본)
2. **fixture 정렬** — 현재 `step7-v7` 로 고정 → #1138 의 not-comparable 해소
3. 커밋된 셀 2개가 다시 비교 가능해짐

즉 **A 없이 B 를 돌리면 quality-only + not-comparable 판정만 나옵니다.**

**권장 순서**: A (`python scripts/qvt_capture_baseline.py`, ~70분)
→ B (`--t0-smoke`, ~2.2 h). B 는 `|graded Δ| < 0.05` 면 거기서 멈추는 게이트라
나머지 18시간을 아낍니다.

⚠️ **cloud tier 는 끄고 갈 것을 권합니다.** `JAMES_ENABLE_CLAUDE_BACKEND=1` 이면
L5 의 AUTO_ROUTER 가 in-evidence 가 되지만, claude leg 는 작업 세션과 Max 쿼터를
공유하고 (`feedback_headless_claude_cli_measurement_hygiene`) 2.2 h 내내 물립니다.
#1136 이후로는 not-in-evidence 가 아티팩트에 남으므로 나중에 따로 채울 수 있습니다.

⚠️ **서버 규칙**: 두 스크립트 모두 **자체 서버를 띄웁니다.** CLAUDE.md 조건 2–5
(포트 선점 확인 / pid 로 수명 소유 / `--reload` 없음 / 해제 확인) 는 스크립트가
이미 지킵니다. **조건 1 (operator 승인) 만 남았고, 세션이 넘을 수 없습니다** —
"run the measurement" 로는 함의되지 않는다고 규칙에 명시돼 있습니다.


---

### 8.5 QVT A 완료 — 5축 정본 베이스라인 (2026-09-22)

**`eval/qvt/baseline_6deca66.json`** — schema `qvt-baseline-v3`,
fixture `step7-v7`, n_runs 3, **생성 실패 0/54**.

| 축 | median | noise band |
|---|---:|---:|
| path_coverage | 0.8636 | 0.0455 |
| graded_answer | 0.7333 | 0.0667 |
| abstention_f1 | 0.5000 | 0.0714 |
| **token_cost** | 1525.35 | 343.45 |
| **latency_cost** | 130.215 | 3.40 |

굵은 두 축이 **rule #2 가 기다리던 것**입니다 — 5월 3축 베이스라인엔 없어서
#1137 이 "없는 축을 flat 으로 채점" 하던 바로 그 축들입니다. 이제 실제
band 를 갖고, α-5 Pareto 판정의 비용 절반이 처음으로 작동합니다.

**⚠️ 5월 베이스라인과 Δ 로 비교하지 마세요.** fixture 가 다릅니다
(`step7-v5` → `step7-v7`, 쿼리 추가됨 — #1138 이 막는 교차-fixture 델타).
이건 5월 것을 **대체하는 새 기준선**이지 5월 대비 변화량이 아닙니다.

**모델 고정 (operator 결정 option 1)**: `JAMES_LLM_MODEL=gemma4:e4b` +
`JAMES_DISABLE_MODE_AWARE_ROUTING=1` + `JAMES_SETTINGS_USE_DB=0`.
세 개 전부 필요합니다 — 하나라도 빠지면 조용히 뚫립니다 (§8.4 의 #1142).

#### 첫 시도는 폐기했습니다

61 분 진행 후 중단. audit_log 실측: **17 질의 중 9 건이
`답변 생성에 실패했습니다.`**, `gemma.timeout` 54 건, 질의당 175–303 초
(호출당 타임아웃 90 초). 원인은 모드 인식 라우팅이 `gemma3:12b` 로 보낸 것.

🔴 **왜 치명적인가**: `pipeline.py:321` 이 그 문구를 no-data 트리거로 보고
softener 가 **기권으로 변환**합니다. 즉 타임아웃한 LLM 이 "올바르게 답변을
거절한 시스템" 으로 채점돼 `abstention_f1` 과 `graded_answer` 를 동시에
오염시킵니다. 캡처는 bench 파일이 없을 때만 실패하므로 **그대로 정본으로
기록했을 것**입니다. #1141 이 이제 채점 전에 중단시킵니다.

#### 서버 규칙 이행 기록

조건 1 승인 · 2 포트 선점 확인 (FREE, python 0) · 3 pid 로 수명 소유
(run 간 teardown 확인, 리스너 항상 1개) · 4 **종료 후 RELEASED + 고아 0
확인** · 5 `--reload` 없음. `taskkill //IM python.exe` **미사용**.
중단 시에도 아티팩트 0 건 — 손상된 베이스라인은 남지 않았습니다.

#### B (T0 smoke) 는 중단됐고, 러너를 수리했습니다 (#1149)

`python scripts/qvt_ablation_matrix.py --t0-smoke` 를 2026-09-22 에 시작해
**완료 질의 7 건 중 4 건 (57%) 이 `답변 생성에 실패했습니다.`** 인 상태에서
중단했습니다. 손상된 셀은 **0 건** 기록됐습니다 (run 1 이 끝나기 전 중단).

**원인 — 매트릭스 러너가 캡처 스크립트가 받은 수리를 하나도 받지 못했습니다.**

| 가드 | `qvt_capture_baseline.py` | `qvt_ablation_matrix.py` (09-22) | 지금 |
|---|:---:|:---:|:---:|
| `JAMES_SETTINGS_USE_DB=0` | ✅ | ✅ (2026-06-15) | 공용 |
| `JAMES_DISABLE_MODE_AWARE_ROUTING=1` | ✅ #1142 | ❌ | 공용 #1149 |
| 생성 실패 시 채점 전 중단 | ✅ #1141 | ❌ | 공용 #1149 |
| provenance (실제 모델) | ✅ #1143 | ❌ | 공용 #1149 |
| 호스트 상태 기록 | ✅ #1147 | ❌ | 공용 #1149 |

kill-switch 가 없으니 모드 인식 라우팅이 매 셀의 `JAMES_LLM_MODEL` 을 덮어
`gemma3:12b` 로 보냅니다. 즉 **5 개 티어 (1b/4b/e4b/12b/27b) 가 모두 같은
모델을 측정하고, 셀 JSON 에는 티어 라벨이 그대로 적힙니다** — `config._llm_setting`
docstring 이 경고한 *"tier-labelled-but-wrong-modeled result"* 그 자체입니다.
러너는 DB 경로 (2026-06-15) 는 막았지만 라우팅 경로 (#969–#990, 2026-06-16)
가 **그 다음 날** 생겼습니다.

그리고 실패 중단 가드가 없으니 57% 실패가 **기권으로 세탁된 채 셀로 기록**됩니다.

⚠️ 세션 중 한 번 이 경보를 **철회했다가 되돌렸습니다.** 첫 확인 때 Ollama 에
`gemma4:e4b` 가 떠 있었는데, 그건 서버 부팅 워밍업이었고 **완료 질의가 0 건**
이었습니다. 질의가 돌기 시작하자 라우팅이 12b 를 끌어왔습니다. 교훈: 아직
측정되지 않은 상태를 반박 증거로 쓰지 말 것.

**수리 (#1149, 2026-09-24)** — 복사가 아니라 **공용 모듈**
`eval/qvt/capture_integrity.py` 로 뺐습니다. 두 스크립트가 나란히 복사된
구조 ("copied from qvt_capture_baseline.py") 가 이 결함의 원인이었으니,
다시 복사하면 같은 표류가 재발합니다. 호스트 상태는 기존 공용
`eval/qvt/host_state.py` 를 씁니다.

- 셀 스키마 **v5 → v6** (가산). `resolved_models` / `answer_health` /
  `host_state` 추가. 실패가 하나라도 있으면 **셀 JSON 을 쓰지 않고** 중단 —
  반쯤 쓴 셀은 `--resume` 과 `--render-report` 가 측정으로 읽습니다.
- 중단 규칙 `abort_reason()` 도 공용입니다. 그러면서 캡처 스크립트의 빈틈
  하나가 드러났습니다: 벤치 파일이 **파싱 불가**면 `failed == 0` 으로 읽혀
  "건강" 판정이었습니다. 이제 두 스크립트 모두 중단합니다.
- M_CLOUD 셀은 `resolved_models.synth_override` 에 cloud 백엔드를 적습니다.
  로컬 태그만 적으면 답을 쓰지 않은 모델이 "effective" 로 기록됩니다.
- **기존 셀 10 개는 이 결함과 무관합니다** — 전부 2026-06-02/03 캡처로,
  모드 인식 라우팅 (06-16) 이전입니다.
- ⚠️ **검증 범위**: 서버·벤치·오라클을 mock 한 행동 테스트
  (`tests/test_qvt_matrix_integrity_guards.py`) + 전체 스위트 5,436 통과.
  **라이브 실행은 아직 없습니다** — 첫 T0 smoke 의 run 1 배너에
  `routing pin: {...: '1', ...: '0'}` 이 찍히는지, 셀 JSON 의
  `resolved_models.effective.tag` 가 티어 모델인지 확인하세요.

### 8.6 지연 분해 arc (2026-09-22 → 09-24, #1145–#1147)

운영자 질문 *"실무에 쓸 수준인가"* 에서 출발했습니다. 가장 단단한 반대
근거가 질의당 130–290 초였고, 그 시간이 어디로 가는지를 잰 arc 입니다.

| PR | 내용 |
|---|---|
| **#1145** | `scripts/research/latency_decomposition.py` — audit_log 로 질의 타임라인 복원. 재실행 가능, 읽기 전용 |
| **#1146** | `JAMES_QUERY_REWRITE_TIMEOUT_S` 노브 (**기본값 10 s 불변**) + paired arm 측정 |
| **#1147** | `eval/qvt/host_state.py` — 캡처가 **호스트 상태를 기록** (GPU 샘플링 · Ollama 적재 모델 · CPU), 스키마 v4 |

**1. 검색은 1% 입니다.** retrieve + rerank 가 질의당 1.4 초. 이 시스템이
느린 건 검색이 아니라 **LLM 오케스트레이션** 때문입니다. 관측된 타임아웃
값 `[5, 7, 10, 12, 20, 22, 30, 32, 45]` 는 소스의 선언 예산 (rewriter 10 /
planner 20 / verify·critique 30 / revise 45) 에 전부 매핑됩니다.

**2. rewriter 노브는 작동합니다 — 기본값은 안 바꿨습니다.** 같은 세션
paired (n=1): 30 s 에서 rewriter 타임아웃 4/26 → **0/24**, 전체 타임아웃
7 → **0**. synth 를 대조군으로 순서 효과를 걷어내면 **노브 순수 효과 ≈
4.7 s/질의** (arm A 의 타임아웃 비용 5.1 s 와 일치). 기본값 변경은 **n=3
같은 세션 확인 후** — abstention +0.167 은 질의 **하나** 가 바뀐 것입니다.

**3. 🔴 `baseline_6deca66.json` 의 latency 축은 날짜를 넘어 비교할 수
없습니다.** 하루 안 band 3.40 s, 이틀 사이 변동 ~57 s. 코드와 무관한
synth 가 09-22 33.8 s → 09-24 14.2 s (2.4×). **09-22 가 왜 느렸는지는
기록이 없어 영영 알 수 없습니다** (CPU 오프로드 가설은 검증 후 기각).
token_cost 는 괜찮습니다. #1147 이후 캡처는 호스트를 기록합니다 —
**latency 주장에는 같은 세션 대조 arm 을 함께 돌릴 것.**

#### 이 arc 에서 틀렸던 것 — 세 번

| 주장 | 무엇이 틀렸나 |
|---|---|
| #1145 헤드라인 "rewriter 83% 실패 / 타임아웃 32%" | **09-22 하루의 호스트 상태.** 같은 예산으로 09-24 는 15.4% / 6.3%. 리포트 맨 앞에 erratum |
| "예산을 늘리면 ~11 초 회수" (#1145 §5) | 느린 호스트 기준 수치 |
| 그 정정 "예산을 늘려도 시간을 되찾을 수 없다" | **추론이었고 틀림.** 타임아웃은 예산을 다 쓰고 **재시도까지** 부르므로, 자연 완료 시간이 옛 예산을 살짝 넘으면 늘리는 쪽이 빠릅니다 |

세 번 모두 **측정이 추론을 이겼습니다.** 두 번째와 세 번째는 서로를
정정한 것이고, 진짜 숫자는 paired run 이 줬습니다.

#### 3단계 결과 — 인지 단계 on/off 대조 (2026-09-24, n=1 스크리닝)

`reports/research-runs/cognitive-stages-contrast-20260924.md`. 같은 세션
back-to-back, 호스트 상태 동일 (SM 클럭 2716 / 2740 MHz). 플래그 적용은
audit_log 로 확인 (arm C 의 plan / verify / reflect / retry = 0 건).

| | arm A (on) | arm C (off) |
|---|---:|---:|
| latency (s/query) | **74.1** | **26.4** (−64%) |
| timeout | 7 | 0 |
| path / graded / abstention | 0.909 / 0.667 / 0.500 | 0.818 / 0.733 / 0.571 |

품질 차이는 질의별로 전부 추적했습니다. **인지 단계 탓으로 추적된 것은
2 건이고, 둘 다 synth 가 맞힌 답을 망친 제품 결함입니다**:

- 🔴 **reflect 가 근거 없이 사실을 판정** (Q14) — critique / revise
  프롬프트에 `{query}` `{draft}` 만 들어가고 검색 자료가 없습니다
  (`reflect/loop.py:213`, `:243`). 모델 학습 이후의 사실 (2026-04 GPT-6
  발표) 을 "치명적 환각" 으로 판정 → revise 가 삭제. verify 는 이를
  `grounded: false` 로 **정확히 잡았지만 annotate 만** 해서 오답이 나갔습니다.
- 🔴 **verify 가 입력용 인젝션 패턴을 출력에 적용** (Q17) —
  `_policies.py:106` `(context|system|previous)\s*(is|was|=|:)\s*["']` 가
  *the context is "<문서 제목>"* 을 인젝션 echo 로 보고 정답을 차단.

path 의 1 건 하락 (Q9) 은 세 단계보다 앞선 LLM 엔티티 추출의 실행 변동이고,
나머지 graded 변화는 synth 문장 변동입니다. **이 결과로 "인지 단계를
끄자" 는 허가되지 않습니다** (n=1 · 단일 suite · 설계 의도 축 미측정).

#### 남은 것

1. ~~reflect 근거 결함 수리~~ — ✅ **#1151 (2026-09-26)**. 상세 =
   `reports/research-runs/reflect-evidence-grounding-20260926.md`.
   critique 가 자료 기준으로 사실을 판정하고, 자료가 없으면 사실 판정
   항목을 뺍니다. critique 의 자료 예산은 `JAMES_SYNTH_CONTEXT_CHARS` 에
   묶어 synth 보다 적게 볼 수 없게 했습니다. **Q14 = pre-fix 4/4 잘못된
   기권 → post-fix 3/3 정답** (paired arm 7개, arm 을 커밋으로 분리).
   QDC 판정은 규칙대로 **`zero`** (graded 밴드 안, 회귀 없음) — 근거는
   집계가 아니라 Q14 열입니다.
   ⚠️ **측정이 중간 산출물 2건을 기각했습니다** — ① 첫 수정본이 "Alex
   Karp"(Palantir CEO) 환각을 만들어 가드 추가 ② revise "근거 없으면
   삭제" 규칙은 기권 다이얼 이동이라 revert (Q20 을 잃고 Q17 은 못 고침).
2. **verify 수리 3건** (신뢰 경계 코드, 단독 PR, auto-merge 금지):
   - echo 패턴 분리 (§4.2 Q17) — `_policies.py:106` 의 입력용 인젝션
     패턴이 출력 검사에 쓰여 정답을 차단.
   - `MIN_ANSWER_LEN_FOR_VERIFY = 30` (`verify.py:286`) — 짧은 오답이
     검증을 통째로 건너뜀. "Alex Karp" 는 9자라 한 번도 검사 안 됨.
   - `context[:2000]` (`verify.py:349`) vs synth 의 8000 — 못 본 뒷부분이
     받쳐 주는 주장이 "근거 없음" 으로 주석될 수 있음. #1151 이 critique
     쪽에서 막은 것과 **같은 모양이 바로 옆 단계에** 남아 있습니다.
3. **n=3 같은 세션 대조** — 2 착륙 후. 그 전에 인지 단계 기본값·예산
   (구 2단계) 을 바꾸지 마세요. ⚠️ `graded_answer` 는 실행 간 20개 중
   9개가 양방향으로 뒤집힙니다 — n=1 집계로 품질 주장 금지.
4. ⚠️ `core/reasoning/reflect/prompts.py` = **17.4 KB / 20 KB** (rule #5).
   프롬프트를 더 넣기 전에 분할하세요.
5. ~~매트릭스 러너 수리~~ — ✅ #1149 (§8.5). T0 smoke 재실행은 서버 승인 후.
6. rewriter 기본값 flip — n=3 확인 후.

## 9. 다음 세션이 하지 말 것

- ❌ **§4.3 의 "3/4" 를 "4/4" 로 올려 쓰기.** 사전등록표를 적힌 대로
  적용한 결과입니다. 깨진 것은 baseline 순서이고, 그 사실을 지우면
  이 프로젝트가 9건의 catch 로 세운 정직한 프레이밍 룰을 깹니다.
- ❌ **`.zenodo.json` 편집.** 예치 원문입니다 (§6).
- ❌ cloud leg 를 작업 세션과 **같은 쿼터 창**에서 돌리기 (§5).
- ❌ 측정 arc 를 **핸드오버 없이** 닫기 (§2).
- ❌ Fork A 확정 전 도메인 코드·토큰 추가 (rule #1).
- ❌ **#1149 이전 커밋의 `qvt_ablation_matrix.py` 로 셀 캡처하기** (§8.5).
  티어 라벨과 실제 모델이 어긋나고, 실패가 기권으로 세탁된 셀이 기록됩니다.
  셀 JSON 이 `qvt-ablation-cell-v6` 가 아니면 그 셀의 `model` 은 라벨일 뿐입니다.
- ❌ **측정 가드를 한 스크립트에만 추가하기.** `eval/qvt/capture_integrity.py`
  에 넣고 두 스크립트가 import 하게 하세요 — 그 표류가 §8.5 의 원인입니다.
- ❌ **다른 날 측정한 latency 를 `baseline_6deca66` 과 직접 비교하기** (§8.6).
  같은 세션 대조 arm 없이는 호스트 차이를 측정하는 것입니다.

---

**작성**: 2026-09-21 · **대상**: 다음 세션
**직전 핸드오버**: `docs/handovers/v0.6.1-close-2026-09-10.md`
**Phase 정의**: `docs/handovers/v0.6.2-restart-roadmap-2026-09-03.md` §2
