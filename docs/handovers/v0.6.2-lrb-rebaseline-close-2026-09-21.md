# v0.6.2 — LRB 재베이스라인 arc 마감 (#1121–#1128)

> **다음 세션 진입점.** rule #6 에 따라 사이클 상태의 원본은 이 문서입니다.
> Phase 정의 원본은 `v0.6.2-restart-roadmap-2026-09-03.md` §2,
> 사이클 마감 실측은 `v0.6.1-close-2026-09-10.md` 가 계속 유효하고,
> 이 문서는 그 **§6 결정 #1 / #7 을 실행한 결과**와 그 뒤에 남은 것을
> 담습니다.
>
> **측정 구간**: 2026-09-12 → 09-14 (PR #1121–#1128)
> **작성**: 2026-09-21 — 측정 종료 **7일 뒤**. 그 지연 자체가 §2 입니다.
> **CI**: `tests` / `lint` / `security-scan` 전부 success
> (`0ad04b9`, run 34774021077 · 4m10s)

---

## 1. 한 줄 요약

`v0.6.1-close-2026-09-10.md` 가 operator 판단으로 남겨 둔 **LRB 결정
#1 · #7 (발표 수치 재베이스라인)** 을 2026-09-12 에 승인받아 실행했고,
그 과정에서 **측정 위생 결함 3건**을 잡아 재실행한 뒤, S3 publication
4-leg cross-model 을 **사전등록된 판정표 그대로 ⭐⭐ partial (3/4)** 로
닫았습니다. 발표 수치가 바뀌었고, **JAMES 주장은 바뀌지 않았습니다.**

---

## 2. 이 문서가 7일 늦은 이유 — rule #6 이 실제로 실패한 지점

기록해 둡니다. 다음 세션이 같은 실패를 반복하지 않도록.

#1121–#1128 은 **자체 핸드오버 없이** 머지됐습니다. 그동안 상태 서술은
두 군데로 흘러갔습니다.

1. `CLAUDE.md` 의 "Where we are right now" 불릿 — 09-12 / 09-13 / 09-14
   의 측정 수치와 PR 번호가 본문에 직접 쌓였습니다.
2. `v0.6.1-close-2026-09-10.md` §7 의 마지막 불릿 — 한 문단이 계속
   덧붙어 **한 줄 600자를 넘는 각주**가 됐습니다 (그 문서가 담기로 한
   것은 09-10 시점 마감인데, 09-14 까지의 진행이 끼어 들어갔습니다).

**가드는 이것을 잡지 못합니다.** `test_v06_claude_md_entry_pointer` 의
불변식 #4 는 *"CLAUDE.md 첫 행이 디스크에서 파일명 날짜가 가장 최신인
핸드오버를 가리키는가"* 를 검사합니다. 새 핸드오버를 **안 만들면**
가장 최신은 여전히 09-10 이고, 포인터는 거기를 가리키고 있으니
**초록입니다**. 가드는 "포인터가 낡는 것"은 막지만 "핸드오버를 아예
안 쓰는 것"은 막지 못합니다 — rule #6 이 금지한 두 번째 행동이 정확히
후자입니다.

이번 PR 은 그 상태를 이 문서로 끌어내고 두 원본을 포인터로 되돌립니다.
가드 강화는 하지 않습니다: "마지막 커밋 날짜 대비 핸드오버가 N일
이상 낡으면 실패" 같은 규칙은 문서 전용 PR 과 유휴 구간을 구분하지
못해 거짓 적색을 만듭니다. 대신 이 §2 를 남겨 다음 세션이 **측정 arc
를 닫을 때 핸드오버를 함께 쓰도록** 합니다.

---

## 3. PR 표 (#1121–#1128)

| PR | 날짜 | 성격 | 내용 |
|---|---|---|---|
| #1121 | 09-12 | docs | v0.2.3b LLM-grounded S3 publication 3-model 결과 + **§4 자기 catch** (project-lead 천장) |
| **#1122** | 09-12 | fix | **S3.2** — `build_lrb_scenario_s3.py::make_project` 제목 충돌 수리 + S3 발표 수치 재베이스라인 + 생성기 가드 |
| **#1123** | 09-12 | fix | reranker **fallback 계수** — 질의별/셀별 조용한 토큰 순서 낙하를 세어 `result.json` 에 기록 |
| **#1124** | 09-12 | fix | claude reranker — repo 밖 cwd (CLAUDE.md 미적재) + 짧은 system prompt + UTF-8 + 세션 파일 없음 |
| #1125 | 09-12 | docs | **결정 #2 실행** — S2 를 수리 fixture 에서 재베이스라인, 4-model leg 재측정 |
| **#1126** | 09-12 | fix | claude reranker — 쿼터 소진 시 **fallback 대신 대기·재시도** |
| #1127 | 09-13 | docs | S3.2 fixture 에서 로컬 3-model 재측정 (9,000 행 **fallback 0**) |
| #1128 | 09-14 | docs | **claude leg** + 4-leg composite + preprint §4.7 |

**`core/` 0 라인.** `git diff --stat v0.6.1..main -- core/` = 빈 출력.
이 arc 는 전부 측정 측 (`eval/external/lrb/`, `scripts/research/`,
`tests/`, 문서) 입니다 — rule #2 의 벤치 게이트 대상이 아닙니다.
92 파일 / +9,364 / −168 (대부분 커밋된 `result.json`).

---

## 4. 무엇이 측정됐나

### 4.1 S3.2 — 생성기 결함 수리 (#1122)

#1121 §4 의 **자기 catch**: S3 publication fixture 의 project-lead
질의 191건 (19.1 %) 이 `make_project` 의 period-20 산술 탓에 제목
충돌을 안고 있어 **어떤 SUT 도 R@1 0.288 을 넘을 수 없는 천장**이
있었습니다. 발표된 token-mode 0.845 도 같은 fixture 였습니다.

수리 후 fixture sha `cc98e141…` — **330/330 고유 프로젝트 제목,
모호한 질의 텍스트 0**. 생성기 가드는 `tests/test_lrb_s3_generator.py`
(+82 라인).

| S3 publication, token mode | V | N | **J** | J − N |
|---|---|---|---|---|
| 발표값 (2026-06-12, 수리 전) | 0.502 | 0.721 | 0.845 | +0.124 |
| **S3.2 (2026-09-12)** | **0.566** | **0.827** | **0.984** | **+0.157** |

천장은 **N 과 J 를 같은 값으로 누르고 있었으므로** 판정에 보수적으로
작용했습니다 — 수리는 gap 을 줄이지 않고 **키웠습니다**.

### 4.2 S2 — 수리 fixture 재베이스라인 (#1125)

결정 #2. #1089 이 S2 생성기의 정책 제목 충돌을 수리한 뒤 저장소가
재현하는 값과 발표값이 갈라져 있었습니다. **token 셀만 고치면 논문의
cross-model 표가 저장소가 재빌드할 수 없는 fixture 위에 남으므로**,
4-model LLM-grounded 도 수리 fixture (sha `2c1b210c…`) 에서 재측정.

| S2 R@1 | token | gemma4:e4b | gemma3:12b | mixtral:8x7b | claude |
|---|---|---|---|---|---|
| vanilla | 0.2500 | 0.4750 | 0.4500 | 0.3625 | 0.5250 |
| naive-supersede | 0.5875 | 0.7375 | 0.7250 | 0.6875 | 0.7750 |
| **JAMES** | **0.7625** | **0.9000** | **0.9000** | **0.8625** | **1.0000** |
| V<N<J | ✓ | ✓ | ✓ | ✓ | ✓ |
| **J − N** | **+0.1750** | +0.1625 | +0.1750 | +0.1750 | +0.2250 |

**5/5 leg 전부 V<N<J**, J−N 은 발표값 대비 ±0.04 이내 (80 질의 중
±3건). 절대 JAMES R@1 은 +0.025 … +0.175 올랐습니다 — 수리된 fixture
에서는 같은 숫자를 가진 낡은 정책 문서가 1위를 뺏지 못하기 때문입니다.

### 4.3 S3 publication 4-leg composite (#1127 / #1128)

12,000 LLM-grounded 행 (로컬 9,000 + claude 3,000), **fallback 0**.

| S3 pub R@1 | token | gemma4:e4b | gemma3:12b | mixtral:8x7b | claude-haiku-4-5 |
|---|---|---|---|---|---|
| vanilla | 0.566 | 0.554 | 0.634 | 0.550 | **0.852** |
| naive-supersede | 0.827 | 0.819 | 0.813 | 0.754 | 0.833 |
| **JAMES** | **0.984** | **0.989** | **0.991** | **0.893** | **0.990** |
| V<N<J | ✓ | ✓ | ✓ | ✓ | **✗ (V > N 0.019)** |
| **J − N** | +0.157 | +0.170 | +0.178 | +0.139 | +0.157 |

**사전등록 판정표 그대로 적용: 3/4 leg clear → ⭐⭐ partial-tier
cross-model + 1건 attribution finding.** 4/4 PUBLICATION-TIER 는
주장하지 않습니다. 보조 조건 (J−N > +0.10) 은 **4/4 충족**.

🔴 **깨진 것이 무엇인지 정확히**: 깨진 것은 **두 베이스라인의 순서
(V vs N)** 이지 JAMES 기여가 아닙니다. J > N, J > V 는 10개 LLM-grounded
셀 전부에서 성립하고 J−N 은 +0.139 ~ +0.225 입니다.

**원인 (fixture 텍스트 × reader 강도)**: S3 의 모든 대체 문서는 본문에
`Supersedes <old id>` 를 적습니다 (260/260 SUPERSEDE 이벤트). **S2 는
0/48**. 강한 reader 는 그 문장을 읽고 vanilla 가 보유한 낡은 판본들
사이에서 현재 판본을 1위로 올립니다 — claude 에서 vanilla 의 현재
상태 카테고리가 1.00/1.00/1.00 (로컬 reranker 는 0.00–0.46). Naive 는
이미 히스토리를 지워서 이득을 볼 수 없습니다 (R@1 = R@10 = 0.833).
S2 에서는 그 문장이 없어 claude 도 V<N 을 유지합니다 (0.525/0.775/1.000).

**크기**: V − N = 0.019 = 1000 질의 중 19건, reranker ≈10 pp 밴드
(#819) 안 — "뒤집혔다"가 아니라 **구분 불가**입니다.

**하지 않은 것**: 대체 문장을 뺀 S3.3 변종이면 reader 강도와 fixture
문구를 분리할 수 있지만, 벤치마크 측 변경이므로 preprint §7 future
work 로만 올렸습니다.

---

## 5. 측정 위생 3건 — 이 arc 의 진짜 교훈

두 번의 claude leg 실행을 **폐기**했습니다. 폐기 판단을 가능하게 한 것이
#1123 입니다.

| # | 결함 | 증상 | 수리 |
|---|---|---|---|
| **#1123** | reranker 가 실패 시 **조용히** 토큰 순서로 낙하 | 763/1000 · 876/1000 · 281/1000 행이 사실상 미측정인데 결과는 정상으로 보임 | 질의별/셀별 fallback 계수 → `result.json` 에 `rerank_fallbacks` |
| **#1124** | `claude -p` 가 repo 안에서 돌아 **CLAUDE.md 가 호출마다 적재** | Max 플랜 쿼터를 태워 셀 중간에 소진 | repo 밖 cwd + 짧은 `--system-prompt` + UTF-8 + 세션 파일 없음 |
| **#1126** | 쿼터 소진 = fallback | 위와 결합해 오염 재발 | **대기 후 재시도** (claude leg 에서 11 × 10분 = 6,600 s 대기, fallback 0) |

**#1123 이 없었으면 오염된 실행이 결과로 발표됐을 것입니다.** 이 프로젝트의
9건 catch 가 만든 "측정을 코드에 맞추지 말라"는 룰과 같은 계열이고,
메모리 `feedback_headless_claude_cli_measurement_hygiene` 에 박혔습니다.

⚠️ **cloud leg 는 세션과 Max 쿼터를 공유합니다.** claude 셀은 ≈3,000 CLI
호출 × ≈20 s + 1.8 h 대기입니다. 다음 cloud leg 를 돌릴 때 같은 창에서
작업 세션을 쓰면 또 소진됩니다.

---

## 6. 발표 수치 — 바뀐 것과 안 바뀐 것

**바뀐 것** (재베이스라인 완료):
- `README.md` / `README.ko.md` / `SUMMARY.md` — S3 publication 수치
- preprint `papers/lrb-preprint/main.tex` §4.6 (S3) · §4 표 3종 (S2) ·
  **§4.7 신설** (cross-scale × cross-model 10셀 + attribution finding)
- `docs/research/lrb-v023-s3-publication-scale-results-2026-09-12-s32.md`
- pre-LOI 자료 (`reports/promo-assets/v0.5-pre-loi-materials/`)
- `benchmarks/README.md` · `docs/evaluation/` 3종

**의도적으로 안 바꾼 것**:
- **`.zenodo.json`** — v0.4.4 예치 **원문 그대로**. 예치된 기록물은
  사후 편집 대상이 아닙니다.
- **`docs/release_notes_v0.4.4.md`** — **erratum 줄만** 추가.
- **v0.4.4 DOI** `10.5281/zenodo.20652679` 는 그대로가 최신 DOI입니다.

---

## 7. 불변식 — 이 arc 종료 시점 실측

| 검사 | 명령 | 결과 |
|---|---|---|
| `core/` 전체 | `git diff --stat v0.6.1..main -- core/` | **0 라인** |
| `core/retrieval` | 같은 명령, 경로 한정 | **0 라인** |
| rule #5 게이트 | `python -m unittest tests.test_v06_module_size_gate` | OK, `GRANDFATHERED` **0건** |
| 엔트리 포인터 가드 | `tests.test_v06_claude_md_entry_pointer` | OK (이 PR 이 첫 행을 이 문서로 이동) |
| main CI | run 34774021077 (`0ad04b9`) | tests / lint / security-scan **전부 success** |

`core/graph` traversal 의 의도된 streak 예외 (lifecycle live-consistency
arc, #1021/#1023/#1024/#1026) 는 v0.6.1 마감 문서 §4.2 의 기록 그대로이며
이 arc 에서 추가 변경은 없습니다.

---

## 8. 남은 일

### 8.1 세션 혼자 가능 — **소진됨 (2026-09-21)**

서버·LLM 없이 세션 혼자 할 수 있는 큐는 이 세션에서 비웠습니다.
남은 것은 전부 operator 입력이 필요합니다 (§8.3).


| # | 항목 | 비고 |
|---|---|---|
| 1 | ~~핸드오버 공백~~ | **✅ 이 문서** |
| 2 | ~~`papers/lrb-preprint/README.md` 상태 낡음~~ | **✅ #1131.** 상태 헤더 재작성 + §4/§5/§6 체크. **그리고 §3 이 실제로 미완인 것을 발견** — 초록은 "three scenarios" 인데 §3 은 "two", Introduction 기여 목록도 "Two pre-registered scenarios" 였습니다. S3 는 §5.7/§5.8/§4.7 에서 측정·논증되는데 **사양이 어디에도 없었습니다** → **✅ #1132** 가 §3 에 S3 작성 (3 preset · 10 카테고리 · S3.2 수리 · `Supersedes` 문장). `% TODO` **1 → 0** |
| 3 | ~~honesty checklist **Item 2**~~ | **✅ #1131.** 인용 **8/8 전부 접근 가능** (대조군 `arXiv:2699.99999` → 404 로 검사기가 실제로 판별함을 확인). 접근성 문제는 0, 대신 **메타데이터 결함 5건**: 🔴 `chu2024timebench` 가 **논문에 없는 저자 3명**을 등재 (10명 → 7명, ACL + arXiv 2311.17667 **직접 확인** 후 제거) · 단독저자에 `and others` · 저널 논문에 `@inproceedings` · 책에 `@article` (출판사가 `journal` 필드) · DOI 4건 누락. 체크리스트 자체도 **존재하지 않는 키** `reflow2026dfah` 를 지목 (실제 `khatchadourian2026dfah`) |
| 4 | ~~유령 PR 2건~~ | **✅ 둘 다 머지 (2026-09-21).** [#687](https://github.com/Hashevolution/James-RAG-Evol/pull/687) — Track 2c Phase 4 분석 (신규 파일 2개, **+889, 순수 추가**). main 이 3곳에서 참조 중이었고 한 핸드오버는 "✅ PR #687" 로 **완료 표시**까지 해 둔 상태였습니다. Phase 5 는 main 에 있는데 그게 기대는 Phase 4 분석만 없었습니다. [#375](https://github.com/Hashevolution/James-RAG-Evol/pull/375) — promo 기록 3행 + placeholder 보충. 🔴 **정정**: 이 표의 이전 판은 *"launch-tracker.md 에는 velog URL 이 있다"* 고 적었는데 **틀렸습니다**. 거기 있던 것은 **K4** 행의 괄호 문구 `(… full at velog.io/@hashevolution)` 였고, **K5 velog · X K6 · Reddit E1 세 행은 main 에 전혀 없었습니다** (세 식별자 전수 검색 0건). 즉 절반 착륙이 아니라 **미착륙**이었고, 머지는 중복이 아니라 누락 보충입니다 |
| 5 | ~~참조 부패 조사~~ | **✅ #1133** + #687 머지. 88 참조 중 10 dangling → **8** (#687 이 2건 해소). CLAUDE.md 표의 죽은 포인터 2건 (bidi 감사 / 존재하지 않는 `scripts/step7_query_test.py`) 을 명시. 7건은 **어디에도 커밋된 적 없음** → 새로 쓰면 복구가 아니라 창작이라 기록만. 상세 §8.2 |

### 8.2 참조 부패 실측 (2026-09-21 전수 조사)

`docs/` · `reports/` · `CLAUDE.md` 가 참조하는 `reports/research-runs/*`
경로 **88개**를 전부 확인했습니다 — **10개가 main 에 없었습니다** (#687 머지 후 **8개**).

| 부류 | 수 | 파일 | 판단 |
|---|---:|---|---|
| ~~열린 PR 안에 있음~~ **✅ 머지됨** | 2 | `track-2c-phase-4-jam-m_m-analysis-20260602.md` · `adversarial-sweep-M_M-20260602T084119.json` | **PR #687** — 2026-09-21 머지, 해소됨. 남은 dangling = **8** |
| **머지 안 된 브랜치에 있음** | 1 | `bidi-normalization-audit-20260602.md` | `docs/v0.4-track-2c-ali-integration` (`c317dda`). 🔴 **`docs/ARCHITECTURE.md` §5.7.12 와 CLAUDE.md 표가 참조**. 이 PR 이 양쪽에 "main 에 없음" 을 명시 |
| **어디에도 커밋된 적 없음** | 7 | `alpha-6-phase-3a-gemma3-{1b,12b,27b}-analysis-20260602.md` · `architecture-md-5-7-12-typed-filter-draft.md` · `qvt-ablation-T0-smoke-analysis.md` · `v3prime-{reflect,verify}-*.json` | `git log --all` 에 이력 0. 핸드오버가 **커밋한 적 없는 파일을 인용**했습니다 |

⚠️ **마지막 부류를 "복구" 할 방법은 없습니다.** 파일이 존재한 적이
없으므로 내용을 새로 쓰면 그건 복구가 아니라 **창작**입니다. 인용하는
핸드오버는 2026-06-01 / 06-02 의 봉인된 문서이므로 이 PR 은 건드리지
않았습니다 — 과거 문서를 소급 수정하는 것과 "참조가 깨졌다"를 기록하는
것은 다른 일이고, 지금 필요한 건 후자입니다.

> `bidi-normalization-audit` 건은 **코드가 없다는 뜻이 아닙니다.**
> `core/input_normalization.py` 와 `tests/test_input_normalization.py`
> (43 tests, green) 는 main 에 있습니다. 착륙하지 않은 것은 *감사
> 보고서* 쪽입니다.

### 8.3 operator 필요

| # | 항목 | 왜 막혀 있나 |
|---|---|---|
| 1 | **QVT 3축** (Graded Answer / Abstention F1 / Token Cost) | 로드맵 **Phase 5 마지막 1건**. 서버 + Ollama, arm 당 ~70분. 디스크 baseline 은 `eval/qvt/baseline_2a31b20.json` (2026-05) 뿐 → **재캡처 먼저** |
| 2 | **dogfood 4건** — 3.1 모바일 긴 질의 / 3.2 detailed / #1084 캡 / #1091 한글 파일명 OCR | 실기기 |
| 3 | **`unpkg.com` 3종 vendoring** → CSP `enforce` 플립 | `frontend/graph.html:367-369` (three 0.160.0 / 3d-force-graph 1.73.4 / d3-force-3d 3.0.5). 현재 `JAMES_CSP_MODE` 기본 `report-only` (`core/security/headers.py:107`). 로컬 우선 원칙상 vendoring 이 답 |
| 4 | **결정 #8 — LRB 제품 전체 셀** | 실서버 + API 로 5,620 이벤트 재생 + `/query/` 시점 매개변수 = `core/retrieval` 변경 (rule #2). 어댑터 JAMES (S2 0.7625) 보다 낮으면 배선 결함. S2 ≈ 2 h / S3 ≈ 1 일 |
| 5 | **`v0.6.1` Zenodo DOI 발행** | 전략 결정 종속 |
| 6 | **`v0.5` 소급 태그** | DOI 계보 변경 |
| 7 | **arXiv 제출** | Item 3 (commit hash 를 `\thanks{}` 에) + Item 4 (LRB 아티팩트를 `10.5281/zenodo.20625533` 에 번들 vs 별도 DOI) = operator 결정 |
| 8 | **TimeQA / TempReason** | 데이터 다운로드 + 라이선스 확인 |
| 9 | **Fork A/B** | 전략. 판정 ≈ **2026-12-13** (약 3개월 남음) |

---

## 9. 다음 세션이 하지 말 것

- ❌ **§4.3 의 "3/4" 를 "4/4" 로 올려 쓰기.** 사전등록표를 적힌 대로
  적용한 결과입니다. 깨진 것은 baseline 순서이고, 그 사실을 지우면
  이 프로젝트가 9건의 catch 로 세운 정직한 프레이밍 룰을 깹니다.
- ❌ **`.zenodo.json` 편집.** 예치 원문입니다 (§6).
- ❌ cloud leg 를 작업 세션과 **같은 쿼터 창**에서 돌리기 (§5).
- ❌ 측정 arc 를 **핸드오버 없이** 닫기 (§2).
- ❌ Fork A 확정 전 도메인 코드·토큰 추가 (rule #1).

---

**작성**: 2026-09-21 · **대상**: 다음 세션
**직전 핸드오버**: `docs/handovers/v0.6.1-close-2026-09-10.md`
**Phase 정의**: `docs/handovers/v0.6.2-restart-roadmap-2026-09-03.md` §2
