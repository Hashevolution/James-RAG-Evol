# v0.6.2 — 재개 로드맵 (Restart Roadmap) · 2026-08-21

> 🟢🟢🟢 **NEXT SESSION ENTRY — 이 문서가 현재 사이클의 단일 진실원
> (single source of truth)입니다.** 새 세션은 CLAUDE.md → 이 문서 순으로
> 읽고 시작하세요.
>
> **왜 이 문서가 생겼나**: 마지막 개발 세션(2026-06-26)과 이 문서
> 사이에 약 2개월의 유휴 구간이 있고, 그 사이 루트 문서 6종
> (`CLAUDE.md` / `SUMMARY.md` / `README*.md` / `ROADMAP.md` /
> `CHANGELOG.md` / `HANDOVER.md`)이 각각 다른 시점에서 멈춰 있었습니다.
> 이 문서는 (1) 그 편차를 사실로 정리하고, (2) 편차를 고친 결과를
> 기록하고, (3) 재개를 위한 단계별 로드맵을 제시합니다.

---

## 1. 지금 상태 (사실 확인, 2026-08-21 기준)

| 항목 | 값 | 근거 |
|---|---|---|
| main HEAD | `73a653a` — `fix(ci): clear the 9 F-class ruff violations blocking the lint gate (#1078)` | `git log -1` |
| 마지막 커밋 | **2026-08-19** (문서/CI 정리 2건) | `git log` |
| 마지막 **기능** 개발 세션 | **2026-06-26** (PR #1062–#1076) | `docs/handovers/v0.6.1-session-close-2026-06-26.md` |
| 실질 유휴 기간 | 약 **2개월** (2026-06-26 → 2026-08-19) | 위 두 행 |
| 최신 PR 번호 | **#1078** | `git log` |
| 공식 릴리스 | **v0.4.4**, DOI `10.5281/zenodo.20652679` | `.zenodo.json` / README badge |
| 사이클 상태 | **v0.5 closed (2026-06-13)** · **v0.6 정식 미진입** (Dim F 게이트 미통과) | `docs/handovers/v0.6-entry-skeleton-2026-06-13.md` |
| 실제 진행된 것 | v0.6 / v0.6.1 **제품 하드닝 스트림** (#886–#1078, 약 190 PR, 릴리스 태그 없음) | `git log`, 아래 §3 |

### 1.1 🔴 P0 발견 — main CI 가 계속 빨간불입니다

문서 어디에도 기록돼 있지 않은 사실입니다.

- `.github/workflows/test.yml` (pytest job) 은 **확인한 최근 60회 실행이
  전부 `failure`** 입니다. 범위: **2026-06-22 → 2026-08-19** (최신 커밋
  #1078 포함).
- 로컬 재현 (`python -m unittest discover -s tests`): **3,694 tests 실행,
  60 failures**. (같은 실행의 771 errors 는 이 컨테이너에 런타임 의존성
  (`fastapi`/`chromadb` 등)이 없어서 난 import 에러이므로 환경 탓 —
  **60 failures 는 파일 내용 assertion 이라 환경과 무관**합니다.)
- 실패 모듈 23개 중 **20개가 CI 의 `--ignore` 목록에 없음** = CI 에서도
  그대로 빨간불.

실패의 성격 (대부분 "코드가 아니라 테스트가 낡음"):

| 실패 모듈 (대표) | 실패 수 | 원인 추정 |
|---|---|---|
| `test_inline_style_extraction` | 13 | PR #1062 인라인 `style=""` → CSS 클래스 이관 후 테스트 미갱신 |
| `test_frontend_event_delegation` | 8 | Claude 스타일 사이드바 개편 (#929–#945) 후 `data-action` 계약 변경 |
| `test_ui_report_polish` / `test_mobile_css_coverage` / `test_mobile_responsive` | 7 / 6 / 3 | 모바일 UI 재작업 (#1064–#1066) 후 셀렉터 변경 |
| `test_a11y_aria_labels` / `test_admin_pw_eye` / `test_brain_icon_shimmer` 등 | 각 1–3 | de-emoji 시리즈 (#998–#1003) 후 이모지 문자열 assertion 잔존 |
| **`test_v06_module_size_gate`** | 1 | **rule #5 실질 위반** — 아래 |

`test_v06_module_size_gate` 는 낡은 테스트가 아니라 **진짜 규칙 위반**입니다:

```
core/response_style.py      22,036 bytes  (cap 20,480 초과 1,556)
core/reasoning/engine.py    21,464 bytes  (cap 20,480 초과   984)
```

CLAUDE.md rule #5 (`core/` 파일 20 KB 상한)를 두 파일이 넘겼고,
grandfather 목록은 비어 있습니다. 즉 **분할하거나 grandfather 등록** 둘 중
하나가 필요합니다.

> **해석 주의**: "CI 가 빨갛다 = 제품이 망가졌다"가 **아닙니다**. 실패의
> 대부분은 UI 계약 테스트가 UI 재작업을 따라가지 못한 것입니다. 다만
> **빨간 CI 는 다음 회귀를 못 잡습니다** — 그래서 재개의 첫 단계입니다.

### 1.2 문서 최신성 편차 (이 PR 이전 → 이후)

| 문서 | 이 PR 이전에 멈춰 있던 시점 | 조치 |
|---|---|---|
| `CLAUDE.md` §"Where we are right now" | **v0.4.3 / Direction α** (2026-06-10 이전) — 실제보다 **약 10주** 뒤처짐 | 현재 상태로 전면 갱신 + "Where to look next" 첫 행을 이 문서로 |
| `SUMMARY.md` §Status | "current cycle = v0.5 **entry**" (2026-06-12) | v0.5 closed + v0.6.1 스트림 + CI 상태 반영 |
| `README.md` / `README.ko.md` | Status badge `v0.5 closed` + 상태 섹션 (2026-06-13) | badge → `v0.6.1 on main (unreleased)` (이 문서로 링크) + 상태 섹션 갱신 + **CI 빨간불 고지 추가** |
| `ROADMAP.md` | 마지막 섹션 = v0.5.x (2026-06-13), "Last updated" = **2026-05-22** | `v0.6.x` 섹션 신설 + Last updated 갱신 |
| `CHANGELOG.md` | 마지막 엔트리 = `[0.5.0-close]` (2026-06-13) | `[0.6.0]` + `[0.6.1]` 엔트리 추가 (#886–#1078) |
| `HANDOVER.md` 헤더 | v0.4.4 (2026-06-12) | 엔트리 포인터 갱신 |
| 가드 테스트 `test_v06_claude_md_entry_pointer.py` | 첫 행이 **존재하는** handover 만 가리키면 통과 → **낡아도 안 잡힘** | **최신성(recency) 불변식 추가** — 아래 §2 Phase 1 |

### 1.3 유휴 시점의 미결 항목 (2026-06-26 핸드오버 §4–§5)

1. **모바일 긴 질의 "Failed to fetch" 드롭** — heartbeat(#1073/#1075)로 완화,
   근본 해결(`/trace/poll` 로 답 전달)은 미착수. **실기기 검증 대기**.
2. **detailed 모드 실사용 확인** — "상세히/전체/원문" 질의가 전체 문서를
   반환하는지 폰에서 확인 대기.
3. **CSP enforce 미완** — HTML 인라인 스타일 596개는 이관(#1062), JS 주입
   ~493개 잔존 → `unsafe-inline` 유지 중.
4. **PaddleOCR 차단** (Python 3.14 휠 없음) — qwen2.5vl:7b 로 우회 완료,
   별도 마이크로서비스는 보류.
5. **★ v0.6 진입 결정 (Fork A / Fork B)** — 2026-06-13 이후 손대지 않음.

---

## 2. 재개 로드맵

```
Phase 1 ── Phase 2 ── Phase 3 ── Phase 4          (solo 가능, 순차)
 문서동기화   CI 그린    부채청산   v0.6.1 마감
                                        │
Phase 5 ─────────────────────────────────┤        (operator-attended, 병렬)
 측정 백로그                              │
                                        │
Phase 6 ────────────────────────────────┴── Phase 7
 전략 분기 결정 (operator)                    v0.6 정식 진입
```

| Phase | 이름 | 실행 주체 | 선행 조건 | 규모(추정) |
|---|---|---|---|---|
| **1** | 문서 최신성 복구 + 재발 방지 | solo | — | **이 PR에서 완료** |
| **2** | CI 그린 복구 | solo | Phase 1 | 1–2 세션 |
| **3** | 유휴 부채 청산 | solo + 실기기 확인 | Phase 2 | 2–4 세션 |
| **4** | v0.6.1 사이클 정식 마감 | solo | Phase 2, 3 | 1 세션 |
| **5** | 측정 백로그 소진 | operator 필요 (LLM 장시간) | Phase 2 | 벽시계 ~20h |
| **6** | 전략 분기 결정 (Fork A/B) | **operator 전용** | 없음 (언제든) | 결정 |
| **7** | v0.6 정식 진입 | Fork 에 따라 분기 | Phase 4 + 6 | 신규 사이클 |

> **규모 추정은 추정입니다.** "세션" = 이전 세션들의 실측 평균(세션당
> 10–25 PR)을 기준으로 한 눈금이며, 계약이 아닙니다.

---

### Phase 1 — 문서 최신성 복구 + 재발 방지 ✅ (이 PR)

**목표**: 새 세션이 어떤 루트 문서를 열어도 같은 현재 상태를 읽게 만든다.

**작업**
- [x] 이 로드맵 문서 신설 = 단일 진실원 + 엔트리 포인터
- [x] `CLAUDE.md` "Where we are right now" 전면 갱신, "Where to look next"
      첫 행 → 이 문서
- [x] `SUMMARY.md` / `README.md` / `README.ko.md` 상태 줄 동기화
- [x] `ROADMAP.md` `v0.6.x` 섹션 신설 + Last updated 갱신
- [x] `CHANGELOG.md` `[0.6.0]` + `[0.6.1]` 엔트리 추가 (#886–#1078)
- [x] `HANDOVER.md` 헤더 포인터 갱신
- [x] **가드 테스트 강화** (`tests/test_v06_claude_md_entry_pointer.py`):
      첫 행이 `docs/handovers/` 안에서 **파일명 날짜가 가장 최신인 문서**를
      가리키는지 검사하는 `test_first_row_points_at_the_newest_handover`
      추가. 새 핸드오버를 만들고 CLAUDE.md 를 안 고치면 **PR 시점에**
      실패합니다. (기존 3 불변식은 그대로 유지.)

**완료 조건**: `python -m unittest tests.test_v06_claude_md_entry_pointer`
그린 + 루트 문서 6종이 같은 상태를 서술.

**재발 방지 원칙 (NEW — CLAUDE.md rule #6)**
> **사이클 상태는 한 곳에만 쓴다.** `docs/handovers/<최신>.md` 가 상태의
> 원본이고, 루트 문서들은 **거기를 가리키기만** 한다. 상태 문장을
> 복제하면 다음 유휴 때 다시 갈라진다.

---

### Phase 2 — CI 그린 복구 (P0)

**목표**: `main` 의 pytest + lint job 을 초록으로 되돌린다. **이것을
끝내기 전에는 새 기능을 만들지 않는다** — 빨간 CI 위에 쌓는 기능은
회귀를 잡아주지 못합니다.

**작업 (권장 순서, PR 단위로 쪼갤 것)**
1. **rule #5 위반 2건 해소** — `core/response_style.py` (22.0 KB),
   `core/reasoning/engine.py` (21.5 KB). 기존 분할 패턴
   (`core/abstraction/`, `core/reasoning/modes/meta/`)을 따라 패키지로
   분할. 분할이 불가하면 `GRANDFATHERED` 에 **분할 계획과 함께** 등록
   (테스트가 요구하는 형식).
   - ⚠️ `core/reasoning/engine.py` 는 rule #2 적용 대상(`core/reasoning`)입니다.
     **순수 이동 리팩터라도** PR 본문에 STEP 7 벤치 수치 + "동작 변경 없음"
     근거(임포트 경로만 변경, 함수 바이트 동일)를 명시하세요.
2. **UI 계약 테스트 재정렬** (~59 failures). 이건 **테스트를 지우는 작업이
   아니라 계약을 다시 쓰는 작업**입니다. 모듈 단위로:
   - `test_inline_style_extraction` (13) — #1062 의 클래스 이관 결과를
     새 계약으로 (인라인 `style=` 0개 + `.u-<hash>` 클래스 존재)
   - `test_frontend_event_delegation` (8) — 현재 `data-action` 목록으로
   - `test_ui_report_polish` / `test_mobile_css_coverage` /
     `test_mobile_responsive` (16) — 현재 셀렉터/브레이크포인트로
   - `test_a11y_aria_labels` / `test_admin_pw_eye` / `test_brain_icon_shimmer`
     / `test_chat_*` / `test_copy_and_conditional_download` /
     `test_threshold_labels` / `test_web_used_badge` /
     `test_graph_visual_rendering` / `test_reasoning_ui_animation` /
     `test_workspace_cr_panel` (각 1–3) — de-emoji / 타이포 개편 반영
   - `test_v06_agent_paths` / `test_v06_llm_settings` /
     `test_measurement_critical_surfaces` / `test_eval_pack_script` — 로컬
     환경(의존성/ruff) 탓인지 실제 회귀인지 **먼저 판별**할 것
3. **판별 규칙 (중요)**: 각 실패마다 *"제품이 틀렸나, 테스트가 낡았나"* 를
   명시적으로 판정하고 PR 본문에 남깁니다. 제품이 틀린 경우가 하나라도
   나오면 그건 **유휴 기간에 숨어 있던 진짜 회귀**이므로 별도 PR + 회귀
   테스트로 처리.
   - ❌ **금지**: 초록을 만들기 위해 테스트를 삭제/skip/xfail 처리하는 것.
     (CLAUDE.md 의 기존 규율과 동일 — "빨간 axis 를 리프레이밍하지 않는다".)
4. **`visual_regression` 베이스라인 재생성** — UI 가 대폭 바뀌었으므로
   `python scripts/visual_regression.py --update-baseline` 로 갱신 후 커밋.
5. **Dependabot / security workflow 알림 정리** — 2개월치 미처리분 확인.

**완료 조건**
- GitHub Actions `test.yml` + `lint.yml` 이 main 에서 **green**
- 로컬 `python -m unittest discover -s tests` 의 failures **0**
  (errors 는 의존성 설치 환경에서 재확인)
- `python -m unittest tests.test_v06_module_size_gate` green

**산출물**: `reports/thaw-2026-XX-XX.md` — 실패 60건 각각의 판정
(테스트 낡음 / 제품 회귀) 표.

---

### Phase 3 — 유휴 부채 청산

**목표**: 2026-06-26 핸드오버가 남긴 open item 을 닫는다.

| # | 항목 | 성격 | 완료 조건 |
|---|---|---|---|
| 3.1 | **모바일 긴 질의 드롭 근본 수리** | 코드 (query/trace + chat.js) | `/query/` 가 즉시 반환하고 답변은 `/trace/poll` 로 전달 → 터널에서 장시간 연결 유지 불필요. 실기기 1회 확인 |
| 3.2 | detailed 모드 실사용 확인 | 실기기 확인 | "상세히/전체/원문" 질의가 원문 표 전체 반환 |
| 3.3 | **CSP `style-src` enforce 완주** | 코드 (대규모, 점진) | JS 주입 인라인 스타일 ~493개 → CSSOM/클래스 이관 후 `JAMES_CSP_MODE=enforce` 플립. 페이지 단위 PR + visual harness 검증 |
| 3.4 | 이미지/OCR 잔여 | 코드 (소) | EasyOCR `cv2 !ssize.empty()` (12MP), 비전 단일이미지 모드 재노출 여부 결정 |
| 3.5 | 운영 룰 문서화 | 문서 | "세션에서 서버를 background 로 띄우지 않는다" (2026-06-26 §3 사고) 를 CLAUDE.md 운영 규약에 명문화 |

**주의**: 3.1 은 사용자 체감 1순위이지만 **실기기 확인이 필요**합니다.
자율 세션 단독으로 "완료" 선언 금지 — 코드까지 넣고 **operator 확인 대기**
상태로 표시하세요.

---

### Phase 4 — v0.6.1 사이클 정식 마감

**목표**: 190개 PR 이 릴리스 태그 없이 떠 있는 상태를 닫는다.

**작업**
- `docs/handovers/v0.6.1-close-<date>.md` — 사이클 종합 (테마별 PR 표,
  불변식 유지 여부, 깨진 streak 의 근거)
- `docs/release_notes_v0.6.1.md` — 릴리스 서술
- 태그 `v0.6.1` + (선택) Zenodo DOI
  - ⚠️ **DOI 발행은 operator 판단**. v0.6 entry skeleton 의 기존 규약은
    "Fork A 진입 또는 Fork B 재평가 시점에 발행"이었습니다. 제품
    하드닝만으로 DOI 를 뽑을지는 전략 결정에 종속됩니다.
- **깨진 streak 명시**: v0.6.1 은 `core/graph` traversal 0-라인 streak 을
  **의도적으로, 측정 근거를 갖고** 깼습니다 (#1021/#1023/#1024/#1026 —
  lifecycle live-consistency arc). 마감 문서에 이 예외를 근거와 함께
  기록해야 이후 세션이 "streak 유지"를 잘못된 사실로 반복하지 않습니다.

**완료 조건**: 태그 + 릴리스 노트 + 마감 핸드오버 3종이 main 에 존재하고,
CLAUDE.md 상태가 "v0.6.1 closed" 로 갱신됨.

---

### Phase 5 — 측정 백로그 소진 (operator-attended, 병렬 가능)

| 항목 | 실행 | 벽시계 |
|---|---|---|
| Graph-RAG 기여도 **Step 2 cross-model** | `scripts/research/graph_rag_synth_step2_cross_model.py` (해석 규칙은 `docs/evaluation/v0.5-graph-rag-contribution.md` §3.2 에 사전 합의됨) | ~14h |
| v0.2.3b LLM-grounded S3 **3-model matrix** | 기존 러너 | 수 시간 |
| D-alce real-NLI verifier + paper v1.4 | 기존 러너 | 수 시간 |
| arXiv preprint 제출 | operator 판단 (Path A defer 상태) | — |

**실행 규칙 (기존 세션 실측 교훈, 반드시 지킬 것)**
- `PYTHONPATH=<repo-root>` 지정
- **`nohup` 금지** (python 이 detach 되어 완료 통지가 안 옴)
- **LLM 실행을 직접 `timeout` 으로 감싸지 말 것** (이전에 v0.2.3b 실행을
  EXIT 124 로 죽인 전례)
- 세션에서 **서버를 background 로 띄우지 말 것** (`:8000` 중복 바인딩으로
  operator 의 요청을 가로챈 전례)

---

### Phase 6 — 전략 분기 결정 (operator 전용)

**v0.5 → v0.6 게이트 = Dim F** (외부 고객 6개월 이상 파일럿) — **미통과**.
2026-06-13 에 고정된 2-fork 계약이 그대로 유효합니다.

| Fork | 트리거 | 그때 열리는 것 |
|---|---|---|
| **A** | 고객 **LOI 체결** | Track D (버티컬 팩 스코핑) + G8.d capability grant workflow + F.2 CR.e 고객 테마 |
| **B** | **6개월 무LOI** → 전략 재평가 | v0.5 모체 결과물을 기준선으로, v0.6 사이클을 새 방향으로 재스코프 |

- 6개월 시계는 2026-06-13 기준 → **판정 시점 ≈ 2026-12-13**.
  2026-08-21 현재 약 **2개월 경과, 약 4개월 남음**.
- **Fork 가 정해지지 않아도 Phase 1–5 는 전부 진행 가능합니다.** 분기가
  막는 것은 Phase 7 과 버티컬 콘텐츠뿐입니다.
- ⚠️ **rule #1 은 Fork A 가 확정되기 전까지 절대 유지**: 법률/식품/유통 등
  도메인 코드·토큰 추가 금지.

---

### Phase 7 — v0.6 정식 진입

Fork 확정 후 새 엔트리 문서를 만들고 시작합니다 (기존 mechanical
checklist = `v0.6-entry-skeleton-2026-06-13.md` §7).

- **Fork A** → `docs/handovers/v0.6-entry-<date>.md` + Track D 착수
- **Fork B** → `docs/handovers/v0.6-reassess-<date>.md` + 새 방향 정의

v0.6 진입 시 대기 중인 solo 작업 큐 (v0.5 close §5 에서 이월, 여전히 유효):
Track A(G1/G2 잔여) · Track B(G8 mount 잔여) · Track C(CSP nonce enforce
= Phase 3.3 과 동일) · Track E(operator-pending) · Track F(mother-level UI).

---

## 3. v0.6 / v0.6.1 에서 실제로 벌어진 일 (#886–#1078 요약)

CHANGELOG 에 없던 구간입니다. 이 PR 에서 `CHANGELOG.md` 에 정식 엔트리로
추가했으며, 아래는 그 요약입니다.

| 묶음 | PR 범위 | 내용 |
|---|---|---|
| 문서/비교 매트릭스 | #886–#889 | v0.6 entry skeleton, 로드맵-README-CHANGELOG 정합 감사, 산업 비교 매트릭스(LangChain/LlamaIndex/Haystack/R2R/ActiveGraph) |
| **P1–P4 운영 하드닝** | #890–#899 | 신뢰 프록시 `X-Forwarded-*` (rate-limit 우회 + audit IP 스푸핑 차단), HTTPS 배포 가이드, per-request 테넌트 미들웨어 + 워크스페이스 경로 리졸버, 5단계 온보딩 · 지식 롤백 · 3-swimlane 추론 시각화 · 용어집 · 한국어 퀵스타트 |
| rule #5 분할 시리즈 | #900–#908 | reflect / gemma_client / replay_graph / _ingestion / _frontmatter / pipeline_synth / graph_engine 패키지 분할 |
| **템플릿(양식) 엔진** | #909–#916 | 도메인 무관 양식 포맷팅 엔진 (등록→적용→다운로드, md/txt/html/docx, 이미지 OCR 인제스트, 챗 모달) |
| **에이전트 트랙** | #918–#921, #1039–#1047 | operator 허용 경로 · `core/agent_tools` · LLM tool-use 루프 · 에이전트 챗 패널 · 폴더 피커/모델 선택 · 클라우드 Claude(Max 플랜 CLI) · `run_shell` (기본 OFF, admin 전용) |
| **LLM 라우팅 통합 + 모드별 측정** | #922, #961–#977 | DB-우선 설정 저장소 + 어드민 UI, `DEFAULT_PREFERENCE` 모드별 분리, chat/retrieval/meta/wiki_edit 3-cell 측정 후 라우팅 확정, 측정 환경 격리 계약 |
| **채팅 UX 전면 개편** | #927–#960 | Claude 스타일 사이드바/세션 팝오버/즐겨찾기, 답변 타이포그래피, 모바일 가독성, 답변 잘림 가드레일 + "계속" 버튼, **SEKOS = 제품 브랜드 / JAMES = 엔진 코드명 확정 (#934)** |
| **프라이버시/비용 상한** | #980–#988 | privacy gate + 월 비용 상한 프리미티브, 클라우드 egress 다층 방어 |
| **UI 통합 (8→5 페이지)** | #998–#1017 | de-emoji, 인트로 프론트도어 (`/` = 소개, `/chat` = 채팅), 그래프 허브 통합, 워크스페이스 "원본 자료" 탭, 답변→추론→그래프 링크 루프, **비주얼 회귀 하네스** |
| **🔴 lifecycle live-consistency arc** | #1018–#1027, #1033–#1034 | 프로브(#1020)가 **라이브 그래프 탐색이 lifecycle status 를 무시**함을 측정으로 증명 (캐스케이드가 라이브 계층에서 사실상 무효였음) → 탐색/스코어/T1 윈도우/3D 스냅샷 전 표면에 `relation_is_live()` 게이트 적용, 시간여행 경로 격리 검증. **측정 근거로 승인된 유일한 traversal streak 예외** |
| 백로그 재측정 | #1028–#1032 | D-alce(샘플링 분산) · HR N=100(**동일 재현**) · v0.2.3b(J−N +0.21 재현) → lifecycle 필터의 회귀 없음 확인 |
| **CSP + 이미지 인제스트 + 롱리퀘스트** | #1062–#1075 | 인라인 스타일 596개 이관, 멀티파일 업로드, 비전 모델 `qwen2.5vl:7b` 전환 + `num_ctx` 8192, 비전→OCR 라우팅, `/query/`·`/upload/` heartbeat 스트리밍, **detailed 답변 스타일** |
| 인용/CI 정리 | #1077–#1078 | v0.3.3 DOI 계보 정정 (Ali Afana 3번째 예치 계기), ruff F-class 9건 해소 |

---

## 4. 유지되는 규칙

CLAUDE.md 의 rule #1–#5 는 그대로입니다. 이 사이클에서 특히 중요한 것:

1. **rule #1 — v1.0 전 도메인 특화 금지.** Fork A 확정 전까지 유효.
2. **rule #2 — `core/{retrieval,graph,reasoning}` 변경 PR 은 벤치 수치 +
   Quality Delta Card 필수.** Phase 2 의 `engine.py` 분할도 대상입니다
   (`fix` 라벨 면제는 측정 산출물 자체를 고칠 때만).
3. **rule #5 — `core/` 20 KB.** 현재 **2건 위반 중** (§1.1).
4. **NEW rule #6 — 상태는 한 곳에만 (state single-source).** 사이클 상태의 원본은 `docs/handovers/` 의 최신 문서 하나이고, 루트 문서는 거기를 가리키기만 합니다. (§Phase 1)
5. **정직한 프레이밍**: 빨간 결과를 초록으로 재서술하지 않습니다. Phase 2
   에서 테스트를 지워 초록을 만드는 것은 명시적 금지입니다.

---

## 5. 재개 첫 세션 체크리스트 (30분)

```bash
# 1. 최신 main 확보
git fetch origin main && git checkout main && git pull origin main

# 2. 의존성 (2개월 뒤이므로 재설치 권장)
pip install -r requirements.txt

# 3. 현재 빨간불 재현 — Phase 2 의 출발점
python -m unittest discover -s tests -p "test_*.py" 2>&1 | tail -5
python -m unittest tests.test_v06_module_size_gate

# 4. 엔트리 포인터 가드 (Phase 1 산출물)
python -m unittest tests.test_v06_claude_md_entry_pointer

# 5. 결정론 벤치가 여전히 재현되는지 (제품 건강도의 진짜 지표)
bash benchmarks/run_all.sh
```

4번과 5번이 통과하는데 3번만 빨갛다면 → **제품은 살아 있고 테스트가
낡은 것**이며, 로드맵 Phase 2 를 그대로 진행하면 됩니다.

---

## 6. 하지 말 것

- ❌ Phase 2(CI 그린) 전에 새 기능 PR 을 쌓는 것
- ❌ 실패하는 테스트를 삭제/skip 해서 초록 만들기
- ❌ Fork A 확정 전 도메인(법률/식품/유통/여행/금융) 코드·토큰 추가
- ❌ 상태 문장을 루트 문서에 복제하기 (rule #6)
- ❌ 세션에서 서버를 background 로 띄우기 (`:8000` 중복 바인딩 사고)
- ❌ 측정 실행을 `nohup` 또는 자체 `timeout` 으로 감싸기
- ❌ v0.6 "정식 진입"을 코드로 선언하기 — 그건 operator 의 Fork 결정입니다

---

**작성**: 2026-08-21 · **대상**: 다음 세션 (또는 재개하는 본인)
**직전 핸드오버**: `docs/handovers/v0.6.1-session-close-2026-06-26.md`
**사이클 계약**: `docs/handovers/v0.6-entry-skeleton-2026-06-13.md` §4 (2-fork)
