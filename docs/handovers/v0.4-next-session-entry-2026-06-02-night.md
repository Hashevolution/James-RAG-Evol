# JAMES — Next Session Entry (2026-06-02 Night / 2026-06-03 Morning)

> Cold-start orientation for the next Claude Code session. Sealed
> 2026-06-02 23:30 with the α-8 Phase C n=3 paired confirm running in
> background (ETA ~03:35 AM). When you wake up / next session starts,
> n=3 should be complete — go straight to §1 First Commands.

---

## 0. TL;DR (30 sec read)

α-8 ontology typed-filter cycle **CLOSED as ⭐⭐ adopt** at n=1
acceptance (multihop graded Δ +0.0367, abst_f1 Δ +0.1357, R1 mechanism
direct evidence — 5 hallucinations averted = 31% reduction in null-query
errors). α-8 = **first clean α-cycle since α-4** (wrong-fix-averted 0).

**n=3 paired confirm in flight** at session close (background task
`bzah80yd7`, ETA 03:35 AM). When complete, fills the noise band and
graduates ⭐⭐ from "directional + mechanism" to "publishable + safe
for ARCHITECTURE.md".

Side discoveries: **2 path_coverage measurement bugs**. Bug 1 (oracle
fallback) fixed on branch `fix/v0.4-path-coverage-oracle-fallback`
(commit `b3c4562`, ready to push). Bug 2 (bench slug matcher) design
written, deferred to combined PR with bug 1.

**Other prep ready**: Track 2c poison_01 α-8 retest exact-command memo,
ARCHITECTURE.md §5.7.12 draft, ontology gap advisor read-only tool.

## 1. First commands when you sit down

```powershell
cd C:\Project\James-RAG-Evol-v010
git status
git log --oneline -8
git branch
# Should show:
# * fix/v0.4-path-coverage-oracle-fallback
#   main

# 1. Verify n=3 paired confirm finished
ls workspaces\hotpot_eval\reports\research-runs\qvt-ablation-cells\qvt-ablation-cell-C_rag-*-M_M.json | Sort-Object LastWriteTime -Descending
# Expected: both qvt-ablation-cell-C_rag-graph-M_M.json AND
# qvt-ablation-cell-C_rag-ontology-M_M.json with LastWriteTime > 2026-06-02 23:00.
# If only one, n=3 partially completed — check log:
tail reports\research-runs\alpha8-phase-c-multihop-n3-confirm.log
```

If n=3 completed cleanly, proceed to §2. If n=3 hung mid-cell, kill
the leftover server processes (`Get-Process python | Stop-Process`),
restart with `--resume` flag, evaluate when it finishes.

## 2. n=3 result analysis (the load-bearing action)

```powershell
PYTHONIOENCODING=utf-8 python -c @"
import json
g = json.load(open('workspaces/hotpot_eval/reports/research-runs/qvt-ablation-cells/qvt-ablation-cell-C_rag-graph-M_M.json', encoding='utf-8'))
o = json.load(open('workspaces/hotpot_eval/reports/research-runs/qvt-ablation-cells/qvt-ablation-cell-C_rag-ontology-M_M.json', encoding='utf-8'))
print('=== α-8 Phase C n=3 paired Δ (ontology - graph) ===')
print()
print(f'{\"Axis\":<18} {\"graph\":>10} {\"ontology\":>10} {\"Δ\":>10} {\"noise_band\":>12}')
for axis in ['path_coverage','graded_answer','abstention_f1','token_cost','latency_cost']:
    ga = g['aggregate'][axis]
    oa = o['aggregate'][axis]
    d = oa['median'] - ga['median']
    nb = max(ga['noise_band'], oa['noise_band'])
    sign = '+' if d >= 0 else ''
    inside = '✓ inside' if abs(d) <= nb else '✗ outside'
    print(f'{axis:<18} {ga[\"median\"]:>10.4f} {oa[\"median\"]:>10.4f} {sign}{d:>9.4f} {inside:>12} (±{nb:.3f})')
"@
```

### Verdict tree (per design memo §4.1)

| graded Δ (median) | Verdict | Action |
|---|---|---|
| ≥ +0.030 AND outside noise band | **⭐⭐ adopt confirmed** | Land ARCHITECTURE.md §5.7.12 + closure PR |
| ≥ +0.030 BUT inside noise band | **⭐⭐ adopt directional only** | Note caveat in closure PR; do not promote to "publishable claim" tier |
| +0.010 to +0.030 | **tier-gated adopt** | Document per-question-type cells; partial closure |
| < +0.010 | **⭐ operational only** | Document "n=1 was noise; typed filter holds default ON anyway because regression-free" |
| Regression (graded Δ < −0.010) | **REJECT** | 11th wrong-fix-averted; revert typed filter default to OFF |

## 3. Pending work (numbered, picked in order)

### 3.1 path_coverage fix PR (combined bug 1 + bug 2)

**Status**: bug 1 already commit `b3c4562` on branch
`fix/v0.4-path-coverage-oracle-fallback`. Bug 2 fix design at
`reports/research-runs/bench-path-matcher-bug2-fix-design-20260602.md` —
implementation NOT yet applied (would have corrupted n=3 mid-run).

**To do**:
1. Apply Bug 2 fix to `scripts/bench.py:_path_metrics` per the design
   memo Option A (12-line slug helper + slug-intersection in hits
   computation).
2. Add tests in `tests/test_bench_path_metrics.py` mirroring oracle's
   test_source_match patterns but on bench's path_metrics output.
3. Verify by running a tiny bench (1-5 queries on a multihop subset)
   and confirming `path_recall_aggregate.mean_path_recall > 0`.
4. Commit + open PR (combined title: `fix(qvt): path_coverage oracle
   + bench slug normalization`).
5. **Ask user before push** — CLAUDE.md `git push` policy.

### 3.2 ARCHITECTURE.md §5.7.12 land

**Status**: full draft at
`reports/research-runs/architecture-md-5-7-12-typed-filter-draft.md`.
Δ table cells marked TBD pending n=3.

**To do**:
1. Insert at `docs/ARCHITECTURE.md` line ~821 (after §5.7.11 bidi).
2. Fill Δ table from n=3 result (both n=1 and n=3 rows).
3. Update `## 8. Versioning` + `## 9. 한국어 요약` per draft's
   insertion notes.
4. Cross-reference from §5.7.10 (QVT) to the new C_rag-ontology cell.
5. Commit on a new branch `feat/v0.4-alpha8-architecture-section`.
6. PR with `Quality delta: exempt (label: docs)`.

### 3.3 Track 2c poison_01 retest at α-8

**Status**: Prep complete at
`reports/research-runs/track-2c-poison-retest-prep-20260602.md` with
exact command + 4 verdict scenarios + post-evaluation action trees.

**To do**:
1. Verify server clean + start fresh (or use sweep's own server logic).
2. Run sweep command from prep memo (~5-6 min, 18 cases).
3. Compare poison_01 verdict vs Phase 4 baseline (was `manual_review`).
4. Per outcome:
   - `resisted` → ⭐⭐⭐ candidate evidence, update memory + DM Ali
   - `partial` → tier-gated, note in memory
   - `manual_review` still → memory note "typed filter doesn't auto-
     generalize to catalog poisoning"
   - `failed` → URGENT investigation, possibly disable filter

### 3.4 Closure PR (when 3.1 + 3.2 + 3.3 ready)

**Status**: closure analysis doc at
`reports/research-runs/alpha-8-phase-d-closure-analysis-20260602.md`.
Memory at `memory/project_alpha_8_closure_state.md`.

**To do** (this is the load-bearing α-8 PR):
1. Title: `feat(α-8 Phase D): ontology typed-filter closure — ⭐⭐
   adopt + ARCHITECTURE.md §5.7.12`
2. Bundle: ARCHITECTURE.md addition + closure analysis doc cite + n=3
   Quality Delta Card + per-question-type cross-tab + R1 audit summary
3. Quality delta: NOT exempt (this is the load-bearing α-8 Δ PR)

### 3.5 Lower-priority follow-ups

| # | Work | Estimate |
|---|---|---|
| 3.5.1 | Track 2c Ali cross-stack DM after poison_01 retest | 30 min |
| 3.5.2 | Phase 3b proper (5 families × n=3 ≈ 45h) on α-8 baseline | overnight |
| 3.5.3 | 5-tier remeasurement (M_XS/M_S/M_M/M_L/M_XL × n=3, 6-10h) | overnight |
| 3.5.4 | Ontology gap advisor — commit `scripts/research/ontology_gap_report.py` + first weekly run | 15 min |
| 3.5.5 | Cleanup untracked files in `reports/promo-assets/`, `reports/research-runs/v3prime-*.json` | 10 min |
| 3.5.6 | Mother-platform CLAUDE.md update — α-8 closure entry under "Where we are right now" | 10 min |

## 4. Session-closing untracked files (background context)

```
reports/promo-assets/v0.4-qvt-ablation-matrix-20260531T224758.md  (pre-existing)
reports/research-runs/v3prime-e-mode-split-2026053{0,0,0,0}T*.json (pre-existing)
reports/research-runs/alpha-8-phase-d-closure-analysis-20260602.md
reports/research-runs/architecture-md-5-7-12-typed-filter-draft.md
reports/research-runs/bench-path-matcher-bug2-fix-design-20260602.md
reports/research-runs/ontology-gap-20260602.md
reports/research-runs/track-2c-poison-retest-prep-20260602.md
scripts/research/ontology_gap_report.py
workspaces/hotpot_eval/eval/  (auto-created by matrix runner)
```

Decide commit / .gitignore / delete per file. The 6 fresh `reports/`
docs are session artifacts — most belong in the α-8 closure PR or
follow-up branches.

## 5. Memory entries this session

- `project_alpha_8_closure_state.md` (renamed from `_phase_c_step7_null.md`) —
  ⭐⭐ adopt verdict, Δ table, mechanism evidence, 6 open items
- `feedback_path_coverage_measurement_bug.md` — 2 bug diagnosis
- `MEMORY.md` — index updated (Phase C ongoing → closure)

## 6. Background process state at session close

```
task bzah80yd7 (Python)
  - server pid 15708
  - log: reports/research-runs/alpha8-phase-c-multihop-n3-confirm.log
  - cell JSONs: workspaces/hotpot_eval/reports/research-runs/qvt-ablation-cells/qvt-ablation-cell-C_rag-{graph,ontology}-M_M.json
  - n=1 backup at: workspaces/hotpot_eval/reports/research-runs/qvt-ablation-cells/_phase_c_n1_backup/
  - launched: 2026-06-02 22:55
  - ETA: 2026-06-03 03:35
```

If background notification arrived: jump to §2.
If process died mid-run: investigate the log tail, restart with
`--resume` flag (matrix runner picks up where it left off).

## 7. CLAUDE.md updates needed (post-closure)

After α-8 closure PR lands, the `## Where we are right now` section
should be updated to:

```markdown
- **Active theme**: **α-8 ontology typed-filter CLOSED ⭐⭐ adopt**
  (PR #688/#689/#<closure>, 2026-06-02 single-day cycle). multihop
  graded Δ +0.037, abst_f1 Δ +0.136, R1 mechanism direct evidence
  (5 hallucinations averted in 25 null queries = 31%). First clean
  α-cycle since α-4. ARCHITECTURE.md §5.7.12 added. n=3 confirmed.
```

And `Where to look next` first row should change to point to:
`docs/handovers/v0.4-next-session-entry-2026-06-03-AM.md` (the post-
n=3-result handover, which the next session writes).

## 8. Korean handover snippet (한국어)

α-8 Phase C+D 한 세션 단일 day closure. **⭐⭐ adopt at n=1**:
multihop graded Δ +0.037, abst_f1 +0.136, 환각 5개 방지 (16→11 = 31%
감소), R1 mechanism 직접 evidence. α-4 이후 첫 클린 cycle (wrong-fix
-averted 0). n=3 paired confirm background `bzah80yd7` 23:00 launch,
ETA 다음 새벽 03:35. 별도 path_coverage 측정 bug 2개 진단 (1 fix
commit b3c4562 ready, 1 design memo). ARCHITECTURE.md §5.7.12 draft,
Track 2c poison_01 retest prep, ontology gap advisor 시제품 모두
준비됨. **다음 세션 첫 명령 = §1 확인 → §2 결과 분석 → §3.1~3.4
순서로 closure**.

---

*Drafted while n=3 ran. If you're reading this in the morning, n=3
result is already on disk — start with `§1 First commands`.*
