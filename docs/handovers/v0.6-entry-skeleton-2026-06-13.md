# v0.6 Entry Preparation Skeleton (2026-06-13)

> **Status**: **PREPARATION ONLY.** v0.6 has NOT been formally
> entered. This doc inventories what's ready for entry, what
> gating decisions remain operator-pending, and what the next
> session should do when one of the gate scenarios resolves.
>
> **Cycle state**: v0.5 closed (`docs/handovers/v0.5-close-
> 2026-06-12.md` PR #862) + the v0.5 → v0.6 gate (Dim F: ≥6 month
> external customer pilot) NOT cleared.
>
> **Audience**: the next session that reads CLAUDE.md "Where to
> look next" first row. This skeleton is the bridge between the
> v0.5 close handover and the eventual v0.6 entry doc.
>
> **Last addendum**: §2.7 + §3.1 + §5.1 below (2026-06-14). The
> 2026-06-13 inventory in §2.1–§2.6 / §3 / §5 is preserved as the
> baseline; only LANDED markers + a single addendum section have
> been added. If you are the next session, read top-to-bottom — the
> baseline numbers still anchor the cycle frame.

---

## 1. Why this doc exists

v0.5 close (PR #862) shipped with a **two-fork v0.6 entry contract**:

> v0.6 entry requires either:
> 1. **LOI signed** → vertical pack scoping conversation begins.
>    Stream D (legal-vertical or similar) becomes scope-eligible.
> 2. **No LOI within 6 months → reassess** → strategy pivot. v0.5
>    work remains the mother-platform consolidation baseline;
>    v0.6 cycle re-scopes around the next strategic direction.

Neither has resolved. The interval is a productive **"v0.5 closed,
v0.6 not yet entered" state** during which mother-platform
hardening continues — and 23 additional PRs (#863 – #885) have
landed since the close handover did exactly that.

The next session needs a place to land that's neither a stale
"close handover" view (which reads as if work stopped at #862) nor
a premature "v0.6 entry doc" (which can't honestly be written
until a gate resolves). This skeleton is that landing place.

---

## 2. What's actually shipped since v0.5 close (#862)

23 PRs landed on `main` between v0.5 close (2026-06-12 PM) and the
present session (2026-06-13). All four mechanical layers of the
rule #1 protection contract (close handover §6.5) held: zero
vertical content, zero `core/retrieval` / `core/graph` traversal
/ `core/reasoning` lines touched, zero domain framing in any PR
title or surface.

### 2.1 Time-Travel Dashboard quartet (Track F.1 complete)

The handover §5.6 F.1 sketch became four shipping PRs that close
the dashboard surface end-to-end:

| PR | Item | Surface |
|---|---|---|
| #865 | **TT.a** | Timestamp picker UI shell + `james:timetravel:*` event dispatch (pre-close handover) |
| #878 | **TT.b** | `GET /admin/graph/reconstruct-at` + audit-replay summary overlay |
| #879 | **TT.c** | `GET /admin/graph/trace-replay` + 3-phase reasoning trail panel (chat.js STAGE_META mirror) |
| #880 | **TT.d** | `GET /admin/graph/diff-vs-now` + side-by-side modal with `chain_extended` projection + audit-evidence deep links |

The dashboard now surfaces the v0.4.2 T5 `reconstruct_graph_at`
primitive + the v0.5 G3 `reconstruct_corpus_view_at` primitive
+ the trace-file replay primitive as a single Time-Travel
operator surface.

**Activation gate**: link-level decoration (`paintLinks` in
`time-travel-renderer.js`) is gated on a join key
(`link.edge_id`) that lands with later T5.A.b mutation-site
wiring. The surface is summary-only until that wiring lands —
documented inline so a future session doesn't re-implement the
already-ready helper.

### 2.2 v0.5 G1 + G2 SaaS-readiness trio (Track A complete)

The handover §5.1 G1.b / G1.c / G2.b / G2.c follow-ups all
shipped. Combined with G1.a (#860) + G2.a (#861) the G1 + G2
contracts are now **complete at the primitive layer**:

| PR | Item | Surface |
|---|---|---|
| #869 | G1.b | `reconstruct_graph_at(..., tenant_id=...)` strict-exclusion filter |
| #870 | G2.b | `apply_change_request(..., approval_evidence=...)` CR merge wire-in |
| #882 | **G1.c** | `docs/deployment/v0.6-saas-tenant-isolation.md` operator deployment guide |
| #883 | **G2.c** | OIDC resolver hook surface (`register_oidc_validator` + env vars) + `with_tenant_id_async` contextvars variant |

**What remains** (LOI-conditional): G2's deployment-side OIDC
validator (depends on which crypto library the customer's IdP is
friendly with). The resolver surface is ready for any
implementation; the bundled validator decision waits on customer
IdP scoping.

### 2.3 v0.6 Pack SDK trio (Track B complete except G8.d)

| PR | Item | Surface |
|---|---|---|
| #875 | SDK.b | `docs/ONTOLOGY_PACK_AUTHORING.md` author guide |
| #876 | SDK.a | `python -m james.pack init <pack_id>` CLI scaffolder |
| #881 | **SDK.c** | `pyproject.toml` + `docs/SDK_VERSIONING.md` (SemVer 12-month deprecation) + build smoke (15 tests) |
| #868 / #871 / #872 | G8.a / G8.b / G8.c | Ontology pack mount mechanism + read-side helpers + replay event types (pre-close handover) |

`python -m build --wheel --sdist` now produces a valid
`james_pack_sdk-0.6.0a1` distribution that contains only the
`james/` subtree — no `core/`, no `routes/`, no test files.

**What remains**: G8.d (capability grant workflow) is LOI-gated
per CLAUDE.md rule #1. Until an operator explicitly grants
`rule_one_exemption_granted`, any pack a third party scaffolds
via `james-pack init` will refuse to mount on a production
JAMES install. The SDK ships ready; the gate stays closed.

### 2.4 Track C — CSP nonce middleware (#884)

Per-request nonce primitive (`core/security/csp_nonce.py`) +
env-flagged composition seam in `build_security_headers`. Two
flags because the two CSP directives have different readiness:

- `JAMES_CSP_USE_NONCE_SCRIPT=1`: safe to set today (zero inline
  scripts after UI #4 PR #855)
- `JAMES_CSP_USE_NONCE_STYLE=1`: **breaks the UI until the 409
  inline `style="..."` attributes are migrated** (UI #6 audit
  Option A or B). Reserved for that future migration cycle.

Default behaviour byte-identical pre-v0.6.

### 2.5 Synthesis + measurement scaffolding

| PR | Item | Surface |
|---|---|---|
| #864 / #877 | Step 1 results | n=3 paired: **Graph-RAG contributes +0.41 path_coverage** (load-bearing, noise band 0.02) |
| #885 | **Step 2 scaffold** | `scripts/research/graph_rag_synth_step2_cross_model.py` driver + synthesis-doc §3.2 pre-agreed interpretation rules |

Step 2 (cross-model M_S + M_L × 3 cells × n=3, ~14h wall) is
operator-launchable with a single command; interpretation rules
are locked **before** the data lands so a future session
interprets by rule rather than post-hoc.

### 2.6 Cumulative

| Metric | v0.5 close (#862) | Post-close additions | Total at this skeleton |
|---|---|---|---|
| PRs landed | 21 (#841–#861) | 23 (#863–#885) | 44 |
| New `core/` modules | (per #862) | +3 (`csp_nonce.py`) + extensions | mother-platform only |
| New tests | ~290 | ~130 (across F.1 + SDK.c + G2.c + Track C + Step 2 scaffold) | ~420 since v0.5 entry |
| `core/retrieval` / `core/graph` traversal / `core/reasoning` lines changed | 0 | **0** | **0** |
| Vertical tokens | 0 | **0** | **0** |

The streak holds.

### 2.7 After-skeleton addendum (2026-06-13 evening + 2026-06-14)

26 more PRs landed between the skeleton write and the 2026-06-14
session — and 2 OPEN PRs from the 2026-06-14 session that haven't
self-merged yet. Same four-layer rule #1 protection contract held;
`core/retrieval` / `core/graph` traversal / `core/reasoning` lines
changed = **still 0**.

**Eight clusters**, all solo + LOI-independent:

| Cluster | PRs | What |
|---|---|---|
| Multi-Phase HTTPS production | #890 / #891 | P1.2 trusted X-Forwarded-* middleware + P1.1 deployment guide |
| Multi-Phase multi-tenant deployment | #892 / #893 / #894 | P3.1 per-request tenant middleware (signed X-Tenant-Id) + P3.2 per-tenant workspace path resolver + P3.3 deployment guide |
| Multi-Phase non-developer UX | #895 / #896 / #897 / #898 / #899 | P4.1 5-step onboarding flow + P4.2 knowledge rollback affordance + P4.3 reasoning flow swimlanes + P4.4 glossary + universal tooltips + P4.5 Korean quickstart |
| Industry-positioning matrix | #887 / #888 / #889 | roadmap + README + CHANGELOG audit → industry comparison matrix → **18-cell verified corrections**. Spawned new memory rule `feedback_external_facing_doc_source_verification_first` (default-negative on certifications, verification log §, lock test, re-verify cadence) |
| Module-size gate enforcement (Rule #5) | #900 / #901 / #902 / #903 / #904 / #907 / #908 | 7 split refactors closing the 20KB cap on `core/reasoning/reflect.py` / `gemma_client.py` / `lifecycle/replay_graph.py` / `wiki_generator/_ingestion.py` + `_frontmatter.py` / `reasoning/pipeline_synth.py` / `graph_engine.py` |
| Template-formatting engine (Track F mother-level UI) | #909 / #910 | 7-PR squashed merge — horizontal document-shaping engine (workspace + chat UI + Tesseract OCR ingest) + session-close handover. **Live e2e smoke pending** (OPEN PR #913 from 2026-06-14 supplies the operator runbook) |
| Dependabot Q2 triage | #911 + 2026-06-14 follow-through | torch CVE-2025-3000 §3 risk-accept + 7-test guardrail. All 4 alerts (13/14 chromadb critical + 17/18 torch low) risk-accepted; 2026-06-14 operator dismiss-as-`not_used` mechanical step prescribed by PR #911 description |
| **NEW solo-doable items from §5 LANDED** | #905 / #906 | CSP nonce script-flag graduation deployment guide + CLAUDE.md "Where to look next" first-row staleness guard test (see §5.1) |

**2026-06-14 session additions (OPEN)**:

| PR | What |
|---|---|
| #912 | `.gitignore` LaTeX build artifacts under `papers/` + preprint zip scope. Closes the 9 untracked LaTeX outputs + 1 zip surfaced in session-open `git status` |
| #913 | `docs/deployment/v0.6-template-engine-smoke.md` operator runbook — 3-path live e2e smoke (workspace golden path + Korean OCR + chat modal) closing PR #910 §4 "Not done — no live LLM/UI run" line. Cross-linked from close handover §5 |

**Operator-attended (not landed, mechanical only)**:

- GitHub Dependabot UI dismissal of alerts #13 / #14 / #17 / #18
  via `gh api -X PATCH ... dependabot/alerts/<n> -f
  state=dismissed -f dismissed_reason=not_used -f
  dismissed_comment="..."`. All four disposition decisions are
  baked into the doc + comment block (PR #911), so the API call
  is the only remaining step.

---

## 3. v0.5 close handover §5 work queue — status sweep

| Item | Source | Status at this skeleton |
|---|---|---|
| **Track A** G1.b replay-side tenant filter | §5.1 | **LANDED** (#869) |
| **Track A** G1.c SaaS deployment doc | §5.1 | **LANDED** (#882) |
| **Track A** G2.b CR wire-in | §5.1 | **LANDED** (#870) |
| **Track A** G2.c OIDC + asyncio | §5.1 | **LANDED** (#883) |
| **Track A** Asyncio `with_tenant_id` variant | §5.1 | **LANDED** (folded into #883) |
| **Track B** G8.a / G8.b / G8.c | §5.2 | **LANDED** (#868 / #871 / #872) |
| **Track B** SDK.a CLI scaffolder | §5.2 | **LANDED** (#876) |
| **Track B** SDK.b PLUGIN_AUTHORING.md | §5.2 | **LANDED** (#875) |
| **Track B** SDK.c PyPI packaging | §5.2 | **LANDED** (#881) |
| **Track B** G8.d grant workflow | §5.2 | **BLOCKED** — LOI required |
| **Track C** CSP nonce middleware | §5.3 | **LANDED** (#884) |
| **Track C** `JAMES_CSP_MODE=enforce` default flip | §5.3 | **PENDING** — gated on UI #6 inline-style migration |
| **Track D** First vertical pack + 6-month pilot + Dim F evidence | §5.4 | **BLOCKED** — LOI required |
| **Track E** arXiv submission re-engagement | §5.5 | operator-pending |
| **Track E** Stream A.3 Hashevolution dogfooding | §5.5 | operator-pending |
| **Track E** Stream A.4 Tier S outreach | §5.5 | operator-pending |
| **Track E** v0.2.3b cross-model runner | §5.5 | operator-pending |
| **Track E** D-alce real NLI + paper v1.4 | §5.5 | operator-pending |
| **Track E** HR full sweep N=100 | §5.5 | operator-pending |
| **Track E** graph-RAG synthesis Step 2 cross-model | §5.5 (implicit) | **scaffolded** (#885); operator launches |
| **Track F.1** TT.a / TT.b / TT.c / TT.d | §5.6 | **LANDED** (#865 / #878 / #879 / #880) |
| **Track F.2** CR.a / CR.b / CR.c / CR.d | §5.6 | **LANDED** pre-close (#866 / #867 / #873 / #874) |
| **Track F.1** Link-level decoration (`paintLinks`) | §5.6 (implicit) | **READY** — gated on T5.A.b mutation-site wiring |
| **Track F.2** CR.e customer-specific theming | §5.6 | **BLOCKED** — LOI required |

**Score**: 20 LANDED + 4 BLOCKED-on-LOI + 6 operator-pending + 2
gated on follow-on work (CSP default flip / link decoration).

**The solo-doable mother-platform work queue from §5 is
substantially exhausted**. What remains is either operator-
attended (Track E), gated on customer LOI (Track D + G8.d +
F.2 CR.e), gated on a separate prerequisite (CSP default flip
needs inline-style migration; link decoration needs T5
mutation-site wiring), or it's a NEW item not in the close
handover.

### 3.1 Update 2026-06-14

The §3 table is preserved as-is for cycle anchoring. No queue item
above has flipped state since the skeleton — both LOI gates and
all 6 operator-pending lines remain unchanged. The 26 PRs in §2.7
sit *outside* this table because they're mother-platform hardening
threads that emerged after the skeleton was written, not entries
from the close handover §5 work queue. Treat §3 as the close-
handover-derived backlog (cycle source) and §2.7 as
opportunistic-but-disciplined hardening (cycle byproduct).

---

## 4. v0.5 → v0.6 gate (Dim F) — still not cleared

Per `PLATFORM_READINESS.md` §3 Dim F + v0.5 close handover §4:

> Dim F gate = ≥6 month external customer pilot + measured
> success metrics.

This is **not cleared**. The two-fork entry contract from §1
remains the only path:

### 4.1 Fork A — LOI signed

If a customer LOI is signed:

1. The next session writes `docs/handovers/v0.6-entry-<date>.md`
   that scopes the vertical pack for that customer (per the
   handover §8 mechanical entry checklist 4).
2. **Track D** becomes scope-eligible. The vertical-pack work
   queue from §5.4 (first vertical pack build → 6-month customer
   pilot measurement → Dim F evidence collection) starts.
3. **G8.d** becomes scope-eligible. The capability grant
   workflow lands with that customer's pack as the first
   `rule_one_exemption_granted` target.
4. **F.2 CR.e** customer-specific theming becomes scope-eligible
   in the post-LOI marketing layer.
5. CLAUDE.md rule #1 stays in force for **any other vertical**.
   Only the LOI-scoped vertical is exempt.

### 4.2 Fork B — no LOI within 6 months → reassess

If no LOI by 6 months after v0.5 close (2026-12-12):

1. The next session writes a strategy-pivot doc in
   `docs/strategy/v0.6-reassess-<date>.md` that names the new
   strategic direction (handover §4 phrasing: "v0.6 cycle
   re-scopes around the next strategic direction").
2. v0.5 mother-platform work stays the baseline.
3. The reassess decision document NAMES what v0.6 entry looks
   like under the new direction.
4. CLAUDE.md rule #1 stays in force — the no-vertical-content
   rule does not loosen because the strategy shifted; only an
   LOI exemption (Fork A) opens a vertical.

**Neither fork should be assumed by the next session**. Both
must resolve via operator decision.

---

## 5. NEW solo-doable items surfaced this session

These weren't in the close handover §5 because they emerged from
the post-close work. Listed for the next session to consider IF
neither gate fork has resolved AND mother-platform hardening
continues:

| Item | Source | Sketch |
|---|---|---|
| **F.1 link-level decoration auto-activation** | TT.b paintLinks helper | When T5.A.b mutation-site wiring lands a `link.edge_id` field on the live snapshot, flip the renderer's `onSet` handler to call `paintLinks(snap)`. ~10 lines of JS + a test. NOT urgent — the summary overlay is operationally useful as-is |
| **UI #6 partial Option B (top-13 pattern only)** | UI #6 audit §3 | Convert the top-13× repeated inline `style="..."` patterns to the 3 utility classes already shipped in `tokens.css`. Requires a screenshot baseline first — defer until a visual-regression harness lands |
| **CSP nonce script-flag graduation doc** | Track C PR #884 | A 1-page deployment doc that documents flipping `JAMES_CSP_USE_NONCE_SCRIPT=1` on production deployments. Safe today (zero inline scripts). Could live next to G1.c (`docs/deployment/`) |
| **v0.6 entry doc skeleton meta-test** | this doc | A test that asserts CLAUDE.md "Where to look next" first row points at the most recent handover skeleton OR entry doc — would catch the staleness this skeleton exists to prevent |

These are **not** a backlog the next session must clear; they're
documented so future-self doesn't have to re-discover them.

### 5.1 Update 2026-06-14 — LANDED markers + new candidates

Status of the four §5 items above as of 2026-06-14:

| Item | Status | PR |
|---|---|---|
| F.1 `paintLinks` auto-activation | **still READY-but-gated** on T5.A.b mutation-site wiring (external prereq, no change since skeleton) | — |
| UI #6 partial Option B (top-13 inline-style → utility classes) | **still pending** visual-regression harness landing (external prereq, no change) | — |
| **CSP nonce script-flag graduation doc** | **LANDED** | **#905** |
| **v0.6 entry-doc staleness meta-test** | **LANDED** | **#906** |

Two of four NEW solo-doable items shipped within the addendum
window. The two still-pending items both share the same shape:
they depend on a separate piece of infrastructure (T5.A.b wiring /
visual-regression harness) that itself isn't in the §5 candidate
list. That's accurate — both should stay pending, not be force-
shipped without the prereq.

**New solo-doable candidates surfaced since the skeleton**:

| Item | Source | Sketch |
|---|---|---|
| Operator-runbook for template engine live e2e smoke | PR #910 §4 "Not done" line | LANDED via PR #913 (2026-06-14 session). Workspace golden path + Korean OCR + chat modal. Operator runs the smoke; the doc tells them how |
| Module-size lock-test extension to wiki/UI surfaces | §2.7 cluster 5 (7 split refactors) | After 7 PR-by-PR splits, a `tests/test_v06_module_size_gate.py` style lock-test that asserts no `core/` file > 20KB could prevent future regression at PR time. Not urgent — the splits were caught manually before merge — but a one-time invariant would close the loop |
| Industry-comparison doc 6-monthly re-verification automation | PR #889 + memory `feedback_external_facing_doc_source_verification_first` | The verification log §9 of `v0.5-industry-comparison.md` prescribes a re-verify cadence (cycle close / competitor major release / annual). A scheduled cron skill or a CI scheduled-run that re-fetches the 6 systems' canonical docs + flags drift could discharge the cadence semi-mechanically. Higher-stakes than the others — touch only if §6 verification rule is itself well-internalized first |
| Dependabot dismissal automation note | §2.7 operator-attended box | The `gh api ... --method PATCH` four-call pattern (with comment text matching the risk-assessment doc section) is mechanical enough to live in a one-shot script under `scripts/security/` for future risk-accept rounds. Not urgent; the 2026-06-14 round is being done by hand. Consider only if a third round surfaces with the same shape |

These new candidates follow the same posture as the original four:
documented for future-self, NOT a forced backlog.

---

## 6. Critical rules (carry-over from CLAUDE.md, unchanged)

The v0.5 cycle close handover §6.5 four-layer rule #1 protection
contract STAYS in force at this skeleton:

| Layer | Mechanism |
|---|---|
| **Code-level** | `rule_one_exemption_granted` capability default empty → `register_pack` raises `CapabilityNotGrantedError` |
| **Doc-level** | Every PR's "Out of scope" section explicitly states vertical content BLOCKED per CLAUDE.md rule #1 until v1.0 / LOI |
| **Naming-level** | UI surfaces use domain-agnostic names — verified across the 23 post-close PRs |
| **Trigger-level** | LOI-gated work tagged in PR description + CLAUDE.md "Where to look next" |

All 5 core CLAUDE.md rules continue. The v0.5 entry doc's 3
additional rules (Dogfooding evidence ≠ Dim F evidence /
Enterprise ontology = mother-level / External-facing claims gated
on measurement evidence) also continue.

---

## 7. Mechanical entry checklist (when a gate resolves)

Per v0.5 close handover §8, updated for the current state:

1. Read this skeleton + the v0.5 close handover + CLAUDE.md
   "Where to look next" first row.
2. **If LOI signed (Fork A)**:
   - Write `docs/handovers/v0.6-entry-<date>.md` formalising the
     LOI scope (per close handover §8 step 4)
   - Pick a Track D item from §5.4 of the close handover OR a
     G8.d / F.2 CR.e item from §5.2 / §5.6 (now unblocked)
   - Update CLAUDE.md "Where to look next" first row to point at
     the new entry doc
3. **If no-LOI reassess decision (Fork B)**:
   - Write `docs/strategy/v0.6-reassess-<date>.md` naming the
     new direction
   - Write a paired `docs/handovers/v0.6-entry-<date>.md` that
     scopes v0.6's work under that direction (Track A/B/C/E
     items still solo-doable; Track D stays blocked)
   - Update CLAUDE.md "Where to look next" first row
4. **If neither (the interval continues)**:
   - Read this skeleton + work on a §5 item from the post-close
     "NEW solo-doable" list OR the close handover §5 items still
     marked operator-pending under Track E
   - No new entry doc required — this skeleton stays the
     baseline until a gate resolves

---

## 8. Operator-pending decisions (priority order, unchanged from close §7)

1. **LOI signed OR "no LOI → reassess"** — gate fork resolution
2. Stream A.3 dogfooding start date
3. Stream A.4 outreach launch
4. CSP graduation timing (Track C nonce flip + UI #6 inline-style migration)
5. v0.2.3b cross-model runner launch
6. **NEW**: graph-RAG synthesis Step 2 cross-model launch (~14h
   wall; same posture as v0.2.3b runner — driver scaffolded
   PR #885)

---

## 9. References

- `docs/handovers/v0.5-close-2026-06-12.md` (the close handover
  this skeleton extends)
- `docs/handovers/v0.5-entry-2026-06-12.md` (v0.5 cycle entry,
  reference for §6 carry-over rules)
- `docs/PLATFORM_READINESS.md` §3 (Dim F gate)
- `docs/evaluation/v0.5-graph-rag-contribution.md` §3.2 (Step 2
  cross-model plan + interpretation rules)
- `docs/deployment/v0.6-saas-tenant-isolation.md` (G1.c
  deployment guide that the next session points enterprise
  procurement at)
- `docs/SDK_VERSIONING.md` (SemVer contract for the published
  `james-pack-sdk`)
- CLAUDE.md rules 1–5 + the v0.5 entry doc's 3 additional rules

**Added 2026-06-14**:

- `docs/deployment/v0.6-csp-nonce-graduation.md` (#905 — §5.1 LANDED)
- `docs/handovers/v0.6-template-engine-close-2026-06-13.md` (#910 feature close handover; §5 cross-linked to the smoke guide)
- `docs/deployment/v0.6-template-engine-smoke.md` (#913 operator runbook; closes #910 §4 "Not done" line)
- `docs/security/dependabot-2026-06-10-risk-assessment.md` (§3 added in #911 — torch CVE-2025-3000 risk-accept)
- `docs/evaluation/v0.5-industry-comparison.md` §9 verification log (#888 → #889 corrections)
- `memory/feedback_external_facing_doc_source_verification_first.md` (new memory rule from the #888 → #889 18-cell correction)
- `tests/test_v06_dependabot_triage_invariants.py` (#911 7-test guardrail asserting zero risky call sites)
- `tests/test_v06_claude_md_entry_pointer.py` (#906 first-row staleness guard — §5.1 LANDED)

---

## 10. Korean summary (한국어 요약)

**v0.6 entry 준비 스켈레톤** (2026-06-13 작성):

- **v0.5 마감 후 23 PR 추가** (#863-#885) — F.1 Time-Travel
  Dashboard quartet 완성 (TT.a/b/c/d) + v0.5 G1/G2 SaaS-readiness
  trio (#860/#861/#869/#870/#882/#883) + v0.6 Pack SDK trio
  (SDK.a/b/c) + Track C CSP nonce middleware (#884) + graph-RAG
  synthesis Step 1 결과 (+0.41 path_coverage n=3) + Step 2
  cross-model driver scaffold (#885). 측정 0, vertical 토큰 0,
  `core/retrieval` + `core/graph` traversal + `core/reasoning`
  0 라인 변경. Rule #1 4-layer 보호 contract 유지.
- **v0.5 close §5 작업 큐 실질 소진**: 20 LANDED + 4 LOI-blocked
  (Track D / G8.d / F.2 CR.e) + 6 operator-pending (Track E) +
  2 prerequisite-gated (CSP default flip / TT.b link decoration).
- **v0.5 → v0.6 gate (Dim F) 미통과**: 둘 중 하나 필요 —
  (Fork A) LOI 서명 → Track D + G8.d 진입 / (Fork B) 6 개월 내
  LOI 없으면 reassess. **둘 다 operator 결정**. 다음 세션은
  결정 전까지 이 스켈레톤이 baseline.
- **다음 세션 진입 절차** (§7): Fork A → `v0.6-entry-<date>.md`
  + Track D 진입 / Fork B → `v0.6-reassess-<date>.md` + 새 방향
  scope / 둘 다 미해결 → 이 스켈레톤 + NEW solo-doable items
  (§5) 에서 진행.
- **NEW solo-doable 후보** (§5): TT.b paintLinks 자동 활성 (T5
  wiring 후) / UI #6 부분 Option B / CSP nonce script-flag
  배포 doc / entry-doc staleness 메타 테스트.
- **Rule #1 4-layer 보호 contract** 그대로 유효. CLAUDE.md
  5 rules + v0.5 entry 3 추가 rules 모두 carry-over.
- **CLAUDE.md "Where to look next"** 첫 행은 이 스켈레톤으로
  업데이트 권장 (별도 PR 또는 이 PR 의 follow-up).

### 10.1 Addendum 한국어 요약 (2026-06-14)

- **스켈레톤 작성 후 26 PR 추가 LANDED** (#886-#911) — 8 클러스터:
  P1 HTTPS production (2) / P3 multi-tenant (3) / P4 non-developer
  UX (5) / industry comparison + 18-cell 정정 (3, 새 memory rule
  `feedback_external_facing_doc_source_verification_first` 박힘) /
  module-size 20KB gate 강제 split (7) / template-formatting
  engine + close handover (2) / Dependabot 확장 (1) / **NEW
  solo-doable §5 LANDED 2건** (#905 CSP graduation doc + #906
  staleness guard). 측정 0, vertical 0, `core/retrieval` +
  `core/graph` traversal + `core/reasoning` 0 라인 변경 streak
  유지.
- **2026-06-14 세션 OPEN**: #912 `.gitignore` LaTeX 빌드 산출물 +
  #913 template engine live e2e smoke 운영자 runbook (workspace
  + Korean OCR + chat modal). 양쪽 self-merge 대기.
- **Dependabot 4건** (chromadb #13/#14 critical + torch #17/#18
  low) **risk-accept 결정 완료** (#911 + 가드레일 테스트 7개).
  CVE 자체는 존재하나 JAMES 사용 방식에서 도달 불가. **GitHub
  UI dismiss-as-`not_used`** 만 operator 수동 4-call API 호출
  필요.
- **§5 새 NEW solo-doable 후보 4건 추가**: module-size lock-test /
  industry-comparison 6-monthly re-verification / template engine
  smoke 가이드 (이미 LANDED #913) / Dependabot 자동화 스크립트.
  모두 "future-self 가 잊지 않게 적어둠" 수준 — 강제 backlog 아님.
- **§3 close-handover work queue 표 자체는 변경 없음**. §2.7 의
  26 PR 은 close handover 큐 항목이 아니라 그 위에서 emerge 한
  hardening 작업. §3 = cycle source / §2.7 = cycle byproduct
  분리.
- **다음 세션 진입 절차** (§7) **변경 없음**. 두 fork 모두 여전히
  operator 결정 대기. 미해결 시 이 스켈레톤 (addendum 포함)이
  계속 baseline.
