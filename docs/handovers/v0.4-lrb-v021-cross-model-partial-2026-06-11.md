# LRB v0.2.1 cross-model — partial measurement (2026-06-11)

> **Status**: producer infrastructure (PR #788) merged + 첫 두 LLM 셀
> 측정 완료. Cell sweep 진행 중 — gemma3:12b × S2 × 3 SUT background
> (~20min) + mxtral / claude operator-attended pending. 본 doc 은
> progressive update — full sweep 완료 시 final handover 별도.
>
> **Honest tier (current partial state)**: ⭐⭐ infrastructure validated
> + capability emergence threshold 측정 (4B vs 12B). ⭐⭐⭐ tier 진입
> 은 S2 rank-order LLM-mode 보존 + cross-model 다중 검증 후만.

---

## 1. 한 줄 요약

LRB v0.2.1 cross-model producer 머지 (PR #788). 측정 시작 후 첫 두
셀 (S1 james × gemma4:e4b + gemma3:12b) 에서 **clean model size
threshold**: 4B = rerank이 가치 부재 (-0.033 R@1 regression), 12B =
marginal lift (+0.022 R@1). literature 정합 capability emergence
패턴.

## 2. 측정 완료 셀

### 2.1 Token-mode 6 cells (Phase B baseline reproduction)

deterministic byte-identical reproduction verified:

| Scenario | SUT | R@1 | R@10 | temporal_acc |
|---|---|---|---|---|
| S1 | Vanilla | 0.6167 | 0.8944 | 0.8944 |
| S1 | Naive-supersede | 0.7389 | 0.9167 | 0.9167 |
| S1 | JAMES | 0.7389 | 0.9167 | 0.9167 |
| S2 | Vanilla | 0.2250 | 0.9500 | 0.9500 |
| S2 | Naive-supersede | 0.5375 | 0.7500 | 0.7500 |
| S2 | JAMES | 0.7125 | 0.9750 | 0.9750 |

→ Phase B baseline 완전 reproduction → 인프라 측정 valid.

### 2.2 LLM-mode 셀 (현재 2 완료, 5+ pending)

#### Cell #1: S1 × james × gemma4:e4b (4B param, Google Gemma family)

| Axis | token | LLM-grounded | Δ (LLM − token) |
|---|---|---|---|
| R@5 | 0.8444 | n/a (per-cell only stored overall) | |
| R@10 | 0.9167 | **0.8833** | **−0.0333** |
| temporal_accuracy | 0.9167 | **0.8833** | **−0.0333** |
| **R@1** | **0.7389** | **0.7056** | **−0.0333** |

Elapsed: 622s (180 queries × 3.5s/query). Honest negative: 4B 모델의
reranker가 token-overlap top-20 위에서 가치 없음, 약한 negative.

#### Cell #2: S1 × james × gemma3:12b (12B param, Google Gemma family)

| Axis | token | LLM-grounded | Δ (LLM − token) |
|---|---|---|---|
| R@10 | 0.9167 | 0.9167 | 0 |
| temporal_accuracy | 0.9167 | 0.9167 | 0 |
| **R@1** | **0.7389** | **0.7611** | **+0.0222** |

Elapsed: 833s (180 queries × 4.6s/query). Marginal lift: 12B 모델이
top-10 내 reorder만 (R@10 unchanged), 단 top-1 정확도 +2.2pp.

### 2.3 Capability emergence ladder

| Model | Size | S1 james R@1 | Δ from token |
|---|---|---|---|
| (token baseline) | — | 0.7389 | — |
| gemma4:e4b | 4B | 0.7056 | **−0.0333** (regression) |
| gemma3:12b | 12B | **0.7611** | **+0.0222** (lift) |
| mixtral:8x7b | 47B (8×7B MoE) | pending | — |
| claude-haiku-4-5 | cloud | pending (operator action) | — |

★ Clean **size threshold finding**: capability emerges between 4B and
12B for LRB rerank task. 4B 미만 = regression risk, 12B+ = positive
contribution. literature 정합 (Schapire 1990 boosting + RAG
"cheat-sheet effect" 패턴 mirror — cycle γ prior art 정합).

★★ Hypothesis 검증 위해 mxtral (47B) 측정 필수 — 47B가 12B보다 더
큰 lift 시 monotonic scaling, 약한 lift / regression 시 plateau /
inverse scaling.

## 3. 진행 중 / Pending

### 3.1 Background running (이 doc 작성 시점)

- **gemma3:12b × S2 × 3 SUT** (~20min ETA): S2 rank-order V<N<J 가
  LLM-mode 에서 보존되는지 검증. ⭐⭐⭐ ladder 의 key leg.

### 3.2 Operator-attended pending

- **mxtral (mixtral:8x7b) S1 + S2 × 3 SUT** (예상 ~2-3h total):
  capability ladder 끝, large local 모델
- **claude-haiku-4-5 S1 + S2 × 3 SUT** (cloud, operator API key 또는
  Max-plan headless `claude -p`): cross-family + cloud point

### 3.3 시점별 의사결정 brunch

| Background 결과 | 권고 다음 action |
|---|---|
| S2 rank-order V<N<J 보존 (+ J 신호 강화) | mxtral 측정 (capability ladder 완성) |
| S2 rank-order 일부 깨짐 (J ≤ N) | root cause cycle (prompt 조정 / k pool size) |
| S2 rank-order 완전 깨짐 (J < N < V) | LLM rerank 가 시간여행 axis 에 부합 안 함 = honest negative |

## 4. Prereg §1.4 honest tier landing — interim

```
LADDER (per prereg §1.4):
  ⭐⭐⭐ = 4 model × 3 SUT axes emit + S2 R@1 rank-order V<N<J
        reproduces in ALL 4 models
  ⭐⭐   = 4 model × 3 SUT axes emit + S2 rank-order 가 3/4 model 에서
        reproduce
  ⭐    = 절반 이하 model 에서 reproduce
```

현재 측정 leg:
- ✅ 2 model × 1 SUT (james) × 1 scenario (S1) axes emit
- ⏳ S2 rank-order 보존 측정 = background (gemma3:12b)
- ⏳ 2 추가 model (mxtral / claude) measurement pending

**Interim tier**: ⭐⭐ partial (infrastructure validated + capability
emergence threshold 측정). ⭐⭐⭐ landing 은 §3.1 background + §3.2
operator-attended 후만.

## 5. 인프라 (PR #788 merged)

- `eval/external/lrb/llm_rerank.py` — Ollama HTTP + claude CLI dispatch,
  deterministic (temp=0, seed=42, format=json), graceful degradation
- `eval/external/lrb/cross_model.py` — `retrieve_at_cross_model` 
  wrapper (token mode pass-through; llm-grounded = adapter top-20 →
  get_doc → rerank → top-k)
- `scripts/research/lrb_run_v021_cross_model.py` — CLI sweep (S1/S2
  auto-detect, token-cell dedup)
- `get_doc(doc_id)` accessor on 3 SUT adapters (read-only)
- 18 tests green

## 6. Re-execution (deterministic given Ollama state)

```bash
# Token-mode reproduction (no LLM needed; <1s)
PYTHONPATH=. python scripts/research/lrb_run_v021_cross_model.py \
  --modes token --models token-baseline

# LLM-mode single cell
PYTHONPATH=. python scripts/research/lrb_run_v021_cross_model.py \
  --modes llm-grounded --models gemma3:12b \
  --suts james --scenarios S1 --timeout 30

# Full operator-attended sweep (~5-10h)
PYTHONPATH=. python scripts/research/lrb_run_v021_cross_model.py \
  --modes token,llm-grounded \
  --models gemma4:e4b,gemma3:12b,mixtral:8x7b,claude-haiku-4-5 \
  --timeout 60
```

## 7. 다음 cycle

Background 완료 후:
1. S2 cells × gemma3:12b × 3 SUT → ⭐⭐⭐ ladder leg 1 확정 또는 결렬
2. mxtral S1 + S2 × 3 SUT → capability ladder 완성 + leg 2
3. claude-haiku-4-5 × S1 + S2 × 3 SUT → cross-family leg 3 (operator)
4. Full handover 별도 (v0.2.1 final) + memory 갱신
5. v0.2.2 GraphRAG SUT track 진입

## 8. 관련

- prereg: `docs/research/lrb-v021-cross-model-preregistration-2026-06-11.md`
- Phase B handover: `docs/handovers/v0.4-lrb-phase-b-cross-scenario-2026-06-11.md`
- design memo: `docs/design/v0.4-lrb-lifecycle-retrieval-benchmark-design.md`
- producer PR: #788
- memory `project_lrb_phase_a_smoke` (Phase A+B 측정 history, v0.2.1
  section 추가 예정)

## 9. Operator action 옵션

`현재 background gemma3:12b × S2 가 finished 후 user 결정 분기`:

(a) **mxtral 측정 즉시 진행** (예상 2-3h, capability ladder 완성)
(b) **claude wiring 시도** (Max-plan headless `claude -p` 또는 API key)
(c) **partial 측정으로 final handover 작성** (mxtral/claude 다음 cycle)

내 권고: **(a) mxtral 진행** — capability ladder finding 의 publishable
강도 ↑ + literature scaling 정합 / 위반 측정 완료. mxtral 결과 후 final
handover.

---

*본 doc 은 progressive update. 새 셀 결과마다 §2.2 + §3 갱신.*
