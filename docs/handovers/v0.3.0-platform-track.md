# v0.3.0 — Platform Skeleton Track Handover

> **Active handover** (2026-05-13~). v0.2 가 정식 종료된 직후 신설.
> 새 Claude Code 세션이 v0.2 → v0.3 진입 이후 처음 열린다면, `CLAUDE.md`
> 다음으로 본 문서를 읽으세요.
>
> **Supersedes**: `docs/handovers/v0.2.0-platform-track.md` (audit
> trail 로 보존).

---

## 1. v0.2 종료 사실

- 2026-05-13 — Axis 6 두 번째 사용자 게이트 통과. 6 axes 모두 완료.
  v0.2 → v0.3 gate clear.
- v0.2 닫는 시점의 자산:
  - 144+ PR, 1709 tests OK
  - Change Request 기반 (`core/change_request.py` + 2 target types)
  - 정공법 3 PR (#252 / #253 / #254): 웹 학습 LLM-triple 위임 +
    UNRESOLVED sweep + 노이즈 cleanup 스크립트
  - 5축 엔지니어링 + cyber UI + monitoring stack + 모든 보안 layer

## 2. v0.3 의 정의 (recall)

`ROADMAP.md` v0.3 섹션 참조. **theme: extension contract 확정**.
도메인 분화는 여전히 v1.0 이후 (CLAUDE.md rule #1 유지).

5 트랙 활성:

| 트랙 | 정의 | 진입점 |
|---|---|---|
| **A** License governance | MIT 유지 + 사전 인프라 + 모니터링 | `session-2026-05-09-license-infrastructure.md` Track A |
| **B** CLA Assistant | 외부 PR 받기 준비 + relicensing grant | 같은 핸드오버 Track B |
| **C** Plugin API skeleton | `core/plugins/base.py` + `packs/general/` + manifest | 같은 핸드오버 Track C |
| **KC** Knowledge Cascade | 5-phase 스키마 진화 + delete/modify + graph editor | `docs/design/v0.3-knowledge-cascade.md` |
| **CR-E** | self-evolution gate → Change Request 래핑 | `docs/handovers/v0.2.x-cr-track.md §5` |

## 3. v0.3 진입 이후 사이클 큐 (2026-05-13 기준)

### 🔴 사이클 1 — 일상 사용 차단 해결 (v0.3 트랙과 독립, 긴급)
- **N-3** 새 세션 리프레쉬 안 됨 — 일상 인사가 안 됨, 이전 세션
  답변이 새 세션에서 나오는 현상. PR #249 (conversation continuity)
  영향 + small-talk RAG 우회 게이트 부재 추정.
- **N-1** 장기기억 후 graph cache invalidation — wiki 에 entity
  추가해도 `/graph` 가 stale, 서버 재시작해야 보임.

### 🟠 사이클 2 — 정공법 라이브 검증 (사용자 직접)
- `python scripts/cleanup_web_noise_entities.py --apply` (현재 6 후보)
- 엔비디아 자료 새로 학습 → NVIDIA org 노드 생성 확인
- "엔비디아 ↔ 조비" edge 라이브 확인

### 🟢 사이클 3 — License Track A 작은 마무리 (PR 묶음)
- A-4 `THIRD_PARTY_LICENSES.md` 단발 자동 생성 (`pip-licenses`)
- A-5 README 3종 라이선스 한 줄 통일 (`README.beginner.ko.md` 라이선스
  줄 추가 포함)
- A-6 첫 분기 모니터링 측정 기록 (`docs/LICENSE_PLAN.md §8` 2026-Q2 행)

### 🟢 사이클 4 — CLA Track B
- B-2 `docs/legal/CLA.md` 본문 초안 (+ A-3 `CONTRIBUTING.md` 동시 PR)
- B-5 `.github/workflows/cla.yml`
- B-7 `CONTRIBUTING.md` CLA 절차
- B-8 거절 응대 템플릿
- 사용자 GUI 액션: B-4 (CLA Assistant 설치), B-5 (`CLA_BOT_PAT`)

### 🟡 사이클 5 — 챗 UX 묶음
- N-4 제안 패턴 일관화 + `**제안**` 칩 가시성
- N-5 부분 추론에서도 웹검색 옵션 (#N-4 와 묶음 가능)
- N-6 장기기억 저장 모달 6c glass + mint 적용

### 🟢 사이클 6 — 운영 도구 + v0.2.x 청소
- `/admin/wiki/resolve-relations` endpoint (정공법 #253 grand sweep)
- `core/memory/store.py` 21 KB → 분할 (CLAUDE.md rule #5)
- `/admin/scheduler/status` + `/jobs/unschedule`
- ruff Phase 2 — F401 / F541 auto-fix

### 🟠 사이클 7 — Plugin API skeleton (Track C, 대규모)
- C-1~C-2 `core/plugins/base.py` 4 인터페이스 + 디렉토리
- C-3 `core/plugins/loader.py` (`JAMES_PLUGINS=` env + SemVer)
- C-4 `pack.yaml` schema (+ `license:` 필드)
- C-5 `packs/general/` 도그푸드 추출
- C-6 `JAMES_WORKSPACE=` 멀티 인스턴스 환경변수
- C-7~C-10 PLUGIN_AUTHORING.md / VERSIONING.md / Eval contract / 도그푸드
  게이트 자동화

### 🟣 사이클 8 — Knowledge Cascade (5 PR)
- Phase A 스키마 마이그레이션 (`sources: [{doc_id, weight, role, ts}]`)
- Phase B 인제스션 경로
- Phase C 삭제 cascade
- Phase D 수정 cascade
- Phase E 그래프 에디터 (= 사용자 요청 N-7)

### 🟣 사이클 9 — CR-E (self-evolution → CR 래핑)
- 디퍼된 v0.2.x §5 항목. 4 locked JSONL test 파일 + eval gate +
  rollback 회귀 위험 → Plugin API 와 같은 시기에 처리.

### 🔵 사이클 10 — 다국어 호환 (N-2, 점진)
- 한국어 ↔ 영어 entity 매칭 율 측정 → synonyms.yaml 자동 보강
  + LLM 추출 프롬프트에 영/한 alias 강제

## 4. 운영 규칙 (변경 없음, recall)

- 도메인 분화 금지 (CLAUDE.md rule #1) — Plugin API 가 v0.3 에서 굳어
  지지만 도메인 pack 은 여전히 v0.4 부터.
- `core/retrieval/graph/reasoning` PR 은 bench numbers 필수 (rule #2).
- 모듈 size gate 20 KB (rule #5). `core/memory/store.py` 21 KB 가
  이미 위반 — 사이클 6 에서 해소.
- 자가-진화 인간 승인 게이트 무조건 유지 (rule #3).

## 5. 한국어 요약

자메스가 정식으로 **v0.3 Platform Skeleton** 단계에 진입했습니다.
v0.2 의 6축 hardening + Axis 6 두 번째 사용자 게이트가 모두 통과.
이제 외부 기여자 받기 인프라(CLA), 플러그인 계약(Plugin API),
지식 진화(Knowledge Cascade), self-evolution 통합(CR-E)이 정식
deliverable. 사이클 1 (일상 사용 차단)부터 차근차근 진행 중.
